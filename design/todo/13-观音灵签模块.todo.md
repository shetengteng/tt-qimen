# 13 · 观音灵签模块 · TODO

> 对应设计文档：[`../modules/13-观音灵签模块.md`](../modules/13-观音灵签模块.md)
> 状态约定：`[x]` 已完成 · `[~]` 进行中 · `[ ]` 待办
> **现状盘点已校对**：2026-04-19

---

## 0. 执行摘要

- ❌ **整模块 0% 实现**
- ✅ **基础设施齐**：路由 `/lingqian` 已注册（`router/index.ts`），无外部库依赖（纯静态资源 + 随机算法）
- ✅ **算法极简**：核心仅 1 行 `Math.random() * 100`（带防连续约束）
- ✅ **复用资产**：`useStorage`（@vueuse/core）记录 lastDrawn、`useDivinationStore`、双主题 CSS、shadcn-vue 基础件
- ⚠️ **现状文件**：仅 `src/modules/lingqian/LingqianPage.vue` 169 字节（占位）
- 🎯 **体验定位**：仪式感最强 — 摇签筒动画 + 签条飞出
- 📊 **真实剩余工时**：约 2.5 天

---

## 1. 类型定义（`src/modules/lingqian/types/index.ts` · **待新建**）

- [ ] `LingqianLevel = '上上' | '上吉' | '中吉' | '中平' | '中凶' | '下下'`
- [ ] `Topic = 'family' | 'marriage' | 'career' | 'wealth' | 'travel' | 'health'`
- [ ] `LingqianItem` — `id(1-100) / level / title / poem[4] / jieyue / xianji / topics(Record<Topic, string>)`
- [ ] `LingqianDrawResult` — `item / drawnAt(timestamp) / animationDuration`

## 2. 核心算法（`src/modules/lingqian/core/lingqian.ts` · **待新建**）

- [ ] `drawLingqian(items: LingqianItem[]): LingqianItem` — 纯随机
- [ ] `drawLingqianSmart(items, lastId)` — 排除上次抽到的签 ID（防连续）
- [ ] `loadLingqianData(): Promise<LingqianItem[]>` — 懒加载 `data/guanyin.json`
- [ ] **使用 useStorage 持久化** lastDrawn ID（`tt-qimen:lingqian:lastId`）

## 3. 数据（`src/modules/lingqian/data/` · **待新建**）

- [ ] `data/guanyin.json` — 100 签完整数据（gzipped ~30 KB）
  - 字段结构对齐 `LingqianItem`
  - **来源 B 类**：开放 API（如 `api.t1qq.com` 等）一次性导出 + 公开站点爬虫清洗
- [ ] `data/levelMap.ts` — `level → 视觉配色 / 等级图标` 映射
- [ ] **构建期校验**：zod schema 校验 100 签全字段非空 + level ∈ 6 档
- [ ] **数据获取流程**：
  - Step 1：从开放 API 拉取 100 签 → `guanyin.raw.json`
  - Step 2：10% 抽样校对（错字 / 标点 / 格式）
  - Step 3：规范化为 `LingqianItem[]`
  - Step 4：发布 `guanyin.json`

## 4. UI 组件（`src/modules/lingqian/components/` · **待新建**）

- [ ] `QianTube.vue` — 签筒 SVG（CSS keyframes 摇晃 1.5s + 签条从筒口飞出动画）
- [ ] `QianNumber.vue` — 签号展示（大字号数字 + 等级徽章）
- [ ] `QianTitle.vue` — 典故标题（宋体大字，如"钟离成道"）
- [ ] `QianPoem.vue` — 签诗 4 行竖排（宋体）
- [ ] `QianJieXian.vue` — 解曰 / 仙机 两栏展示
- [ ] `TopicTabs.vue` — 6 维度 tab（家宅 / 婚姻 / 事业 / 财富 / 出行 / 健康）
- [ ] `RedrawButton.vue` — 再抽一次按钮（含限流提示）
- [ ] **复用** `CollapsibleSection`

## 5. Composables

- [ ] `composables/useLingqian.ts` — `() => { current, draw, redraw, drawCount, isThrottled }`
- [ ] `composables/useDrawCount.ts` — 抽签计数（每日 N 次软限流，超出提示休息但不强制）
- [ ] 接入 `useDivinationStore` 缓存抽签历史（最近 5 次，UI 可查看）

## 6. Page（`src/modules/lingqian/LingqianPage.vue` · **替换占位**）

- [ ] 国风 / 简约 双主题分支
- [ ] 标准布局：QianTube(摇签动画) → QianNumber + QianTitle → QianPoem → QianJieXian → TopicTabs → action-bar(再抽 / 分享 / 保存)
- [ ] 抽签前显示"心诚则灵"提示文案

## 7. i18n 文案

- [ ] `locales/modules/lingqian.zh-CN.ts` — 待新建
- [ ] `locales/modules/lingqian.zh-TW.ts`
- [ ] `locales/modules/lingqian.en.ts`
- [ ] 文案：pageTitle / pageSubtitle / breadcrumb / inputCardTitle("心诚则灵") / btn(draw/redraw/share) / topic(6 名) / level(6 档) / throttleHint
- [ ] **签文本身保持中文原文**（不翻译，仅翻译 UI）

## 8. 路由

- [x] `/lingqian` 路由已注册
- [x] `LingqianPage.vue` 已 lazy load

## 9. 数据工程

- [ ] **Day 1**：100 签数据爬取 + 清洗
  - 来源：≥ 2 个开放 API 或公开站点交叉对照
  - 用 zod schema 在构建期校验 100 签全字段非空
- [ ] **Day 1.2**：10% 抽样人工校对（错字 / 标点 / 典故名 / 等级）
- [ ] **Day 1.5**：规范化为 `LingqianItem[]` + gzip 压缩 + 发布

## 10. 性能

- [ ] `data/guanyin.json` 通过 `() => import('./data/guanyin.json')` 动态懒加载（首屏不加载）
- [ ] 摇签动画用 `transform` + `requestAnimationFrame`（GPU 加速）
- [ ] **目标**：`/lingqian` 路由首屏 gzipped < 100 KB（含动画 CSS / SVG，不含数据 JSON）
- [ ] 数据 JSON 单独 gzip 后 ~30 KB，按需加载

## 11. 测试要点

- [ ] 抽签 100 次 → 全部 100 签都至少被抽中一次（统计学概率）
- [ ] 防连续约束：连续抽签 5 次，无 ID 重复
- [ ] 摇签动画在主流浏览器流畅
- [ ] 6 维度 tab 切换无错位
- [ ] 100 签数据无 level 缺失 / poem 不足 4 句

## 12. 验收清单

- [ ] 100 签数据全部录入 + 10% 抽样审校通过
- [ ] 摇签动画 60fps
- [ ] 防连续约束生效
- [ ] 主题 / 语言切换无破坏
- [ ] 免责声明在签文页面显著位置（合规要求）

## 13. 知识库映射

| 层 | 类型 | 来源 | 当前状态 |
|---|---|---|---|
| 算法 | 自研（< 10 行随机 + 约束） | — | 缺 |
| 100 签全数据 | B 类 | 开放 API + 公开站点爬虫清洗 | 缺 |
| 签文原文 | B 类 | 公版文献（无版权风险） | 缺 |

## 14. 工时估算

| 任务 | 估算 |
|---|---|
| 100 签数据爬取 + 清洗 + 抽样校对 | 1.0 |
| 类型 + `core/lingqian.ts`（含限流） | 0.3 |
| 7 个 UI 组件（含签筒动画） | 1.0 |
| `LingqianPage.vue` + i18n + 联调 | 0.2 |
| **合计** | **2.5 天** |

## 15. 风险与对策

| 风险 | 应对 |
|---|---|
| 不同版本签文差异（关帝签 vs 观音签） | 文档声明采用"观音灵签百度百科通用版" |
| 爬虫数据错漏 | 10% 抽样 + LLM 辅助格式校验 + zod schema 构建期校验 |
| 用户"疯狂抽签" | 软限流：useStorage 记录每日抽签次数，超过 N 次提示休息（不强制） |
| 合规：宗教内容 | 首页 + 页脚 + 签文页都显示免责声明 |
| 签筒动画在低端机型卡顿 | `transform` + `will-change`，必要时降级为静态过渡 |

