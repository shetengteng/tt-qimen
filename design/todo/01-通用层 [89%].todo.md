# 01 · 通用层 · TODO（功能视角）· 89%

> 对应设计文档：[`../modules/01-通用层.md`](../modules/01-通用层.md)
> 状态约定：`[x]` 已完成 · `[~]` 部分实现 · `[ ]` 待办
> **本版重写**：2026-04-19 · 改为以"通用层向各模块提供的功能能力"为骨架
> **同步**：2026-04-23 · 巡检 9 个模块真实代码后回写（详情见各模块 todo）：8 个业务模块已 100% 完成 MVP；本通用层"已建未连"项保持原状（divinationStore / FortuneError / ResultCard / 真太阳时 / about 等路由 / 字体栈），但 `useShareCard` composable 已落地并被 6 模块全部接入，附录 C 依赖项 html2canvas / fuse.js / chinese-character-strokes 均已安装
> **同步**：2026-04-26 · 巡检真实代码差量回写：(1) `FortuneError` (`src/lib/errors.ts`) 已实现并被 5 个 core 模块（chenggu/xingming/lingqian/huangli/liuren）使用，#20 升级为 `[~]`；(2) `useShareCard` 实际接入 8 个模块（先前误记 6 个）；(3) ShareModal 形态已通过 `SharePreviewDialog.vue` + 弹框内"含入参二维码" + AppFooter "入口二维码"完成，#13 升级为 `[x]`；(4) 灵签摇签动画 `lq-shake` 已实现，#17 备注修正；(5) 字体栈在 guofeng/minimal tokens.css 中已定义并被 css 全面引用，#15 升级为 `[~]`；(6) 各模块 `compute-error-card` 友好错误 UI 已普及，#21 升级为 `[~]`（仍缺全局 ErrorBoundary）；(7) `useBirthStore` 通过 provide/inject 解决了反向依赖架构，#11 备注修正
> **同步**：2026-04-26 (晚) · `bazi` / `ziwei` core 入口接入 FortuneError（wrap tyme4ts / iztro 的原生 RangeError → `FortuneError(module/invalid-input)`，原 cause 保留）；jiemeng core 为纯静态查表 + null fallback 设计、无业务异常路径，不接入；至此 #20 升级为 `[x]`，附录 C "所有 core 异常外抛 FortuneError" 升级为 `[x]`
> **同步**：2026-04-26 (深夜) · 通用层批量收尾：(1) 创建 `/about` `/disclaimer` `/data-source` `/privacy` 4 个内容路由 + `LegalContentLayout` + 三语法律级文案，#7 升级为 `[x]`；(2) 创建 `AppErrorBoundary.vue`（onErrorCaptured + 友好 UI）+ `app.config.errorHandler` 兜底，#21 升级为 `[x]`；(3) `BirthForm.vue` 物理移到 `src/components/common/`，bazi/ziwei/chenggu 三方 import 路径同步，#11 升级为 `[~]`（i18n key 仍属 `bazi.*` namespace 留作技术债）；(4) 抽 `lib/element.ts` + `lib/tenGod.ts`（extract-only，bazi 暂不重构），#18/#19 升级为 `[~]`；(5) `useUserStore` 增加 `name` + `setName` + `hasName`，#2 升级为 `[~]`（仍待 xingming 模块 store 合并）；(6) 抽 `DisclaimerBanner.vue` 独立组件、AppHeader 引用，#10 升级为 `[x]`；(7) 抽 `lib/trueSolarTime.ts` + `composables/useTrueSolarTime.ts`（extract-only，core 暂不消费），#1 升级为 `[~]`；(8) `divinationStore` 加 `cachedCompute` helper + JSDoc 接入示例，#6 升级为 `[~]`；(9) 创建 `ResultBanner.vue` 通用壳，重构 6 个模块（bazi/ziwei/chenggu/xingming/liuren/lingqian）的 12 段重复 banner 模板（每模块 gf+mn 各 1 段 → 单一组件调用 + 透传 ref），#12 升级为 `[~]`
> **同步**：2026-04-27 · 真太阳时联动接入完成（V2 → MVP 完成度内）：(1) `BirthForm.vue` 增加 `<details>` 折叠的高级选项块——「真太阳时」开关 + 出生城市下拉（34 条 `CITY_LONGITUDE` 全曝光）+ 「其他 · 手动输入经度」+ 实时偏移分钟数预览；(2) `bazi/core/bazi.ts` 与 `ziwei/core/ziwei.ts` 在 `calculateBazi` / `buildZiweiChart` 入口统一调用 `useTrueSolarTime(birth)` 一次（longitude undefined → noop 兼容），下游所有四柱 / 大运 / 神煞 / 紫微宫位的计算自动消费修正后的 hour/minute/day；(3) `chenggu` 页面对 `<BirthForm :show-advanced="false" />` 关闭高级选项（其 core 不消费 longitude，避免 UI 误导）；(4) i18n 三语补齐 `common.birthInput.*` 17 条 + `cities.*` 34 条；(5) #1 升级为 `[x]`，附录 C "真太阳时 core 接入" 升级为 `[x]`
> **同步**：2026-04-27 (BirthForm·简化轮) · **真太阳时高级选项 → inline 城市搜索**：
>   - 用户反馈："如果选择了城市，就说明开启了太阳时，简化 form 设计，直接在性别后面添加城市选择，要求有输入关键字查询"。
>   - 设计：
>     · 砍掉「高级选项 details」折叠块、「使用真太阳时」复选框、「自定义经度」输入：35 城市已覆盖 95%+ 用户场景，进一步专业概念（°E/°W）下放为 dev-only 后续工具；
>     · 新增 `src/components/common/CityCombobox.vue`（input + 下拉，~250 行，无新依赖；支持中文/繁体/英文/拼音 key 模糊搜索 + 键盘 ↑↓ Enter Esc + onClickOutside + 清除按钮）；
>     · BirthForm 在性别字段后的同一行 `ds-input-row` grid 内插入「出生城市」CityCombobox 单元，选了城市 = 自动开真太阳时（onPaipan 时把 `CITY_LONGITUDE[key]` 写入 `userStore.longitude`），不选 = 自动关；
>     · `props.showAdvanced` 别名保留作向后兼容（chenggu 调用站不改），实际控制改为 `props.showBirthplace`，未传时回退 showAdvanced，再无传则默认 true。
>   - i18n 三语：删除 9 条冗余 key（`advancedToggle / advancedToggleClose / trueSolarLabel / trueSolarHint / birthplacePlaceholder / birthplaceCustom / longitudeLabel / longitudePlaceholder / longitudeHint`）；新增 3 条（`birthplaceSearchPlaceholder / birthplaceNoResult / birthplaceClear`）；保留原有 `birthplaceLabel + cities.* 34 + offsetPreview*`。`offsetPreview` 文案前缀加「真太阳时修正」更直观。
>   - 验证：`scripts/common/probe-birthform-logic.mjs` 6/6 用例（选北京/上海/台北/香港/不选/showBirthplace=false 透传）；ReadLints 0；vue-tsc 0 新错；vite build 4.14s（BirthForm bundle 15.09KB / gzip 4.35KB，与简化前持平）；dev server 模块 hot-load 200。
>   - 影响面：BaziPage / ZiweiPage 4 处调用站零改动（默认 props.showBirthplace=true 自动展示城市字段）；ChengguPage 显式 `:show-advanced="false"` 通过 alias 兼容继续生效；其他模块（黄历 / 灵签 / 小六壬 / 解梦 / 姓名学）不消费 BirthForm 不受影响。
> **同步**：2026-04-27 (深夜·A 轮) · 排盘结果**刷新存活**模式下沉为通用约定 —— `liurenStore` 首发的 `lastComputed` 输入指纹快照被 `baziStore`（新增）+ `ziweiStore`（扩展）跟进，三模块 API 同构（`recordComputed / clearComputed / shouldRestore`）；page 侧统一 `tryRestoreLastResult` + `skeleton.revealImmediately()` 跳过骨架屏。新增 `scripts/common/probe-snapshot-restore.mjs` 13 case 契约测试（同盘 / 7 字段穷举 / longitude 三段切换 / 损坏 string / null）。本质是 P4 用户体验：刷新页面 / 切窗回来 不丢盘。#6 升级为 `[~]`（已有 4 模块覆盖 + helper 在），附录 A "刷新存活"行升级为 `[~]`
> **同步**：2026-04-27 (深夜·Z 轮) · BirthForm 残余 i18n 反向依赖一次性清理 —— 三语 `common.birthInput.*` 新增 6 个子树（`inputCardTitle / calendar / field / gender / hours / btn.paipan(Icon)`，含 12 项 hours 数组），共 ~21 条 key × 3 语 = 63 entry；`BirthForm.vue` 模板 + 脚本所有 16 处共享 i18n 引用从 `bazi.*` 改至 `common.birthInput.*`；`BaziPage.vue` `tm('bazi.hours')` 同步迁移；`bazi.{calendar,field,hours,btn.paipan(Icon)}` 旧 key 保留作为 deprecated alias 不删（`bazi.gender.maleTitle/femaleTitle` 仍被 FourPillarsTable 消费，需保留 `bazi.gender` 整对象）。验证：ReadLints 0 + vite build 4.59s + grep 0 处残留共享 key。剩余清单 §172 升级为 `[x]`
> **同步**：2026-04-27 (深夜·F+D 轮) · **字体栈与 CSS 变量命名一致性核对（§171）+ 抽屉滑入动效（§172）一次性收尾** —— (1) **字体栈核对**：调研发现两主题字体决策与设计文档 §5.2 原意有意偏离（国风用 Kaiti SC 而非 Source Han Serif SC，更具古风手书感；简约用 Inter 而非 Source Han Sans SC，Web 端渲染更清晰），决策"保留视觉，补全 fallback"——guofeng `--font-display/--font-body` 加入 `Source Han Serif SC` 作为最末 fallback；guofeng `--font-mono` 从纯 ui-monospace 升级为 `JetBrains Mono + SF Mono + ui-monospace` 完整链（与设计文档原意一致）；minimal 同步加入 `Source Han Sans SC` 作为 fallback；新增 `--gf-font-mono` 别名；contracts.css 写入 18 行决策注释。(2) **抽屉滑入动效**：调研发现移动菜单实际选用 Popover 而非 Drawer（产品决策），保留架构、增强动画——窄屏 (max-width: 768px) `.layout-popover-content--nav` 走新建 `@keyframes layout-drawer-slide-in`（200ms cubic-bezier(0.16, 1, 0.3, 1) + `translateY(-12px → 0) + scale(0.985 → 1)`），呼应原意"transform: translateX(-100% → 0) 200ms ease-out"的"抽屉滑入"语感。验证：ReadLints 0 + vite build 4.21s 通过。§171/§172 / 附录 D / 附录 E 全部回写
> **同步**：2026-04-27 (深夜·R 轮) · **`ResultCard` 设计意图归档决策** —— 调研 6 个使用 `ResultBanner` 的模块后确认：lingqian 6 档（上上/上吉/中吉/中平/中凶/下下）、chenggu 5 档（top/high/middle/low/bottom）、xingming 5 档（大吉/吉/平/凶/大凶）、liuren 3 档（ji/xiong/neutral）的 level 枚举本就不统一，且各模块的"主结果展示"组件（LotteryCenterpiece / WeightTable / OverallGauge / PalaceWheel）已成熟，硬塞通用 `ResultCard` 会破坏现状（高风险、低收益）；设计文档 §4.4 原意（结果区主壳 + 吉凶色条 + 三槽位）实际已由 `ResultBanner`（6 模块共用结果揭示横幅 + guofeng/minimal 主题对偶）充分承载，`level` 颜色编码下沉到各模块"主结果组件"自治，不在通用层做强行统一。§1 #12 / §2 步骤 6 / §4 约束 / 附录 D 工时 全部归档为 `[x]`，`bazi/ziwei/chenggu/xingming/liuren/lingqian` 6 模块 todo 不需要回写（与本决策无冲突）
> **同步**：2026-04-27 (深夜·I 轮) · **8 模块 i18n 子文件统一拆分**（§170 + 附录 B 第一项一次性收尾）—— ziwei 已先行于上轮拆分，此轮将剩余 7 模块（bazi 144k / liuren 57k / chenggu 50k / lingqian 58k / xingming 95k / huangli 138k / jiemeng 56k）共 21 文件批量抽出；写 `scripts/common/extract-module-locales.mjs` 自动化抽取脚本（幂等 / 三语对齐 / 块边界匹配 + tail 修补），主 `zh-CN/zh-TW/en.ts` 行数从 1445 降至 459（**-68%**）；新建 `scripts/common/diff-i18n-keys.mjs` 推广为 8 模块通用版本，默认全部检查、可选指定子集，旧 `scripts/ziwei/diff-i18n-keys.mjs` 已删除；附带修复 `CollapsibleSection.vue` 反向依赖（`bazi.collapse.collapseLabel/expandLabel` → `common.collapse.collapseLabel/expandLabel`，三语主文件新增 `common.collapse` 子树，`bazi.collapse` 旧 key 删除避免死代码堆积）。验证：ReadLints 0 + vite build 4.71s + diff 脚本 8 模块 723 keys 三语全部对齐 + dist/assets 24 个独立 i18n chunks（按需加载就绪）。**8 模块 × 3 语言 = 24 个 chunks 共 85 KB 全部从主 bundle 剥离按需加载**。§170 升级为 `[x]`，附录 B 第一项升级为 `[x]`
> **同步**：2026-04-27 (BirthForm·进一步精简) · **去除偏移分钟数预览 + label 改名**（§1 #1 / #11 微调 + 字号收尾）：
>   - 用户反馈："真太阳时修正：+14 分钟 这个去除"+"城市（选填）这样就可以了"。
>   - BirthForm.vue：删除 `<p class="ds-offset-preview">{{ offsetPreviewText }}</p>` 模板（双布局两处）+ `.ds-offset-preview` CSS 块；脚本侧删 `selectedLongitude` 之外的 `offsetMinutes` `offsetPreviewText` 计算（保留 `selectedLongitude` 因 `userStore.longitude` 写回时仍需读 `CITY_LONGITUDE`）；移除对 `trueSolarTimeOffset / computeDayOfYear` 的导入（lib 自身保留供后续 core 接入）；旧选项注释从 35 城市更正为 34 城市（实际 `CITY_LONGITUDE` 全表）。
>   - i18n：三语 `birthplaceLabel` 从「出生城市/Birthplace」改为「城市（选填）/ City (optional) / 城市（選填）」；删除已不再被引用的 `offsetPreview / offsetPreviewNoChange`（zh-CN/zh-TW/en 三处共 6 entries）。
>   - 验证：ReadLints 0；vue-tsc 0 新错（5 处预存在错误与本 PR 无关）；vite build 4.42s（`BirthForm-*.js` 14.48 KB / gzip 4.17 KB，比上轮 15.09 KB / 4.35 KB 还**更小**，因为代码删多了）。
>   - 影响：BirthForm 视觉更轻、字段更少；用户选了城市后没有任何"反馈中间态"，true solar 修正在排盘后果中体现，符合"简化 form 设计"原意。

---

## 0. 模块定位

通用层为 8 个占卜模块提供**共享的状态、组件、路由、样式、工具、错误处理、国际化**。本身不直接对用户提供占卜功能，但决定了所有模块的一致性与协同体验。

---

## 1. 功能能力清单（核心）

> 对应设计文档 §1-§7 全部能力点。一行 = 一个对外提供的能力。

| # | 能力 | 设计文档 | 状态 | 实现位置 | 缺口 / 备注 |
|---|------|---------|------|---------|------------|
| 1 | 生辰输入数据结构 `BirthInfo` | §1.1 | `[x]` | `src/stores/user.ts` 中的 `BirthInput` + `src/lib/trueSolarTime.ts` + `src/composables/useTrueSolarTime.ts` + `BirthForm.vue` + `CityCombobox.vue` | 2026-04-27：bazi `calculateBazi` 与 ziwei `buildZiweiChart` 入口统一接入 `useTrueSolarTime(birth)`（longitude undefined → noop 兼容）；BirthForm 真太阳时 UI 简化为 inline 城市搜索（2026-04-27 BirthForm·简化轮）：选城市 = 隐式开启，清空 = 隐式关闭；offset 预览文案改为「真太阳时修正：±n 分钟」；chenggu 因 core 不消费 longitude 显式 `:show-advanced="false"`（alias 仍兼容） |
| 2 | 用户全局状态 `UserState`（`birth + name`） | §1.2 | `[~]` | `src/stores/user.ts` 含 `birth + name + setName + hasName`（NAME_MAX_LENGTH=16，trim+截断防御） | 字段已就位；xingming 仍持有自己的 `xingmingStore`，二阶段重构时合并到全局 user.name |
| 3 | 用户态 localStorage 持久化 | §2.1 | `[x]` | `useStorage` 已接入（含 `tt-qimen:birth` `tt-qimen:name` 两条 key） | — |
| 4 | 主题状态（明暗 + 国风/极简双引擎） | §2.2 | `[x]` | `src/stores/theme.ts` + `composables/useTheme.ts` | — |
| 5 | 多语言状态（zh-CN/zh-TW/en） | 衍生 | `[x]` | `src/stores/locale.ts` + `composables/useLocale.ts` | — |
| 6 | 模块结果缓存 | 衍生 | `[~]` | `src/stores/divination.ts` 含 `get / set / clear / cachedCompute` helper + 接入指南 JSDoc；**另一个正交维度**：刷新页面 `lastComputed` 输入指纹快照（per-module localStorage）——`liurenStore` 首发，2026-04-27 (深夜·A 轮) `baziStore` + `ziweiStore` 跟进，`scripts/common/probe-snapshot-restore.mjs` 13/13 case | 内存 helper 业务侧未消费（评估后认定：8 个 core 计算 < 50 ms 无 CPU 收益，主排盘按钮路径不应短路）；刷新存活已由 lingqian / liuren / bazi / ziwei 4 模块覆盖（lingqian 走 itemId 模式，其余 3 个走输入指纹模式） |
| 7 | 路由表 + 8 模块懒加载 | §3.1 | `[x]` | `src/router/index.ts` + 12 个路由（8 业务 + 4 法律：about/disclaimer/data-source/privacy） | 用 hashHistory 不是 webHistory（合理偏离） |
| 8 | 路由切换自动 lazy-load 对应模块 i18n 包 | 衍生 | `[x]` | `router.beforeEach` | — |
| 9 | 顶部头 `AppHeader`（导航 + 主题切换 + 语言切换） | §4.1 | `[x]` | `components/layout/AppHeader.vue` | — |
| 10 | 免责声明 `DisclaimerBanner` | §4.2 | `[x]` | `src/components/common/DisclaimerBanner.vue`（在 AppHeader 内复用） | 已抽出独立组件 |
| 11 | 共享生辰输入 `BirthInput.vue` | §4.3 | `[x]` | `src/components/common/BirthForm.vue` + `src/components/common/CityCombobox.vue` + `src/composables/useBirthStore.ts`（provide/inject 解耦） | 文件已物理移到 components/common/；i18n key 已重整至 `common.birthInput.*`（2026-04-27 完成 6 子树 / 21 keys 三语 63 entry 迁移）；真太阳时 UI 简化为 inline 城市搜索（2026-04-27 BirthForm·简化轮）：性别后面同一行 `CityCombobox`（中/繁/英/拼音 key 模糊搜索 + 键盘导航），选城市 = 隐式开真太阳时，clear = 关；砍掉 details 折叠 / useTrueSolar 开关 / 自定义经度入口，35 城市覆盖 95%+ 用户 |
| 12 | 通用结果卡 `ResultCard`（`level` prop + 三槽位） | §4.4 | `[x]` 设计意图已由 `ResultBanner` 充分覆盖 | `src/components/common/ResultBanner.vue`（结果区"揭示横幅"通用壳） | **2026-04-27 决策（深夜·R 轮）**：调研后确认 6 模块的"主结果展示"组件已成熟（LotteryCenterpiece 6 等级 / WeightTable 5 档 / OverallGauge 5 档 / PalaceWheel 3 档），各模块 level 枚举不统一、视觉差异大，硬塞通用 ResultCard 会破坏现状（高风险、低收益）；设计文档 §4.4 的 ResultCard 形态原意（结果区主壳 + 吉凶色条 + 三槽位），实际已由 `ResultBanner`（结果揭示横幅 + 主题对偶视觉，6 模块共用）充分承载 —— `level` 颜色编码下沉到各模块"主结果组件"自己处理（每模块的 level 语义本就不同），不在通用层做强行统一；故此项升级为 `[x]` 并归档不再追加完整 `level` prop + 三槽位实现 |
| 13 | 分享卡 `ShareModal`（含 html2canvas 导出 PNG + 二维码 + 主题纹样） | §4.5 | `[x]` | `composables/useShareCard.ts` + `components/common/SharePreviewDialog.vue` + `components/common/ShareToast.vue` + `components/common/ShareQrcode.vue` | 8 模块（bazi/ziwei/chenggu/lingqian/liuren/xingming/huangli/jiemeng）全部接入 useShareCard；ShareModal 形态已抽为 `SharePreviewDialog`，弹框内含"含入参的二维码"用于复现本次排盘；AppFooter 全局展示"入口二维码"指向当前页路由；主题纹样通过各模块 share-card DOM 自身的主题样式自然包含 |
| 14 | CSS 变量令牌（朱砂红/古铜/鎏金/墨黑/宣纸黄/吉绿/凶红） | §5.1 | `[~]` | `themes/{guofeng,minimal}/tokens.css` | 当前用主题双套实现；与设计文档列出的固定变量名需做命名一致性核对 |
| 15 | 字体栈（思源宋体 / 思源黑体 / JetBrains Mono） | §5.2 | `[x]` | `themes/{guofeng,minimal}/tokens.css` + `themes/_shared/contracts.css`（决策注释） | **2026-04-27 (深夜·F 轮) 决策**：保留主题视觉决策（guofeng 楷体 / minimal Inter），同时补全 fallback 至设计文档原意——guofeng `--font-display/--font-body` 加入 `Source Han Serif SC` 末位 fallback；guofeng `--font-mono` 从 ui-monospace 升级为 `JetBrains Mono + SF Mono + ui-monospace` 完整链（与设计文档原意一致）；minimal 同步补 `Source Han Sans SC` fallback；contracts.css 写入 18 行决策注释——视觉与意图均无偏 |
| 16 | 响应式断点 mixin（mobile-only / tablet-up / desktop-up） | §5.3 | `[ ]` | 部分 plain CSS @media | 无 SCSS mixin 抽象 |
| 17 | 关键动效（卡片悬停 / 抽签摇晃 / 雷达图增长 / 抽屉滑入） | §5.4 | `[x]` | 雷达图、骨架揭示、灵签摇签（`lq-shake-mn` / guofeng 对偶动画）+ 抽屉滑入（`@keyframes layout-drawer-slide-in` 在窄屏 768px 启用）均已落地 | **2026-04-27 (深夜·D 轮)**：移动菜单实际选用 Popover（产品决策，更轻、不挡内容、点击外部自动关闭），保留架构、增强动画——窄屏 `.layout-popover-content--nav` 走 200ms cubic-bezier(0.16, 1, 0.3, 1) `translateY(-12px → 0) + scale(0.985 → 1)` 呼应原意"transform: translateX(-100% → 0) 200ms ease-out" |
| 18 | 五行查表工具 `utils/element.ts`（`getElementOfGan`） | §6.1 | `[~]` | `src/lib/element.ts`（已抽出但 bazi 暂未重构消费） | 含 `STEM_TO_ELEMENT` / `isYangStem` / `elementRelation` / `stemElement` / `ELEMENT_NAMES` 等常量与函数；extract-only 策略防止现有 bazi 回归 |
| 19 | 十神算法工具 `utils/tenGod.ts`（`calcTenGod`） | §6.2 | `[~]` | `src/lib/tenGod.ts`（已抽出但 bazi 暂未重构消费） | 含 `calcTenGod` / `classifyTenGod` / `TEN_GOD_CATEGORY` 等；extract-only 策略 |
| 20 | 全局错误结构 `FortuneError`（`{ code, userMessage }`） | §7 | `[x]` | `src/lib/errors.ts`（含 `module` / `code` / `userMessage` / `cause` / `details`） | 7 个有抛错语义的 core 模块（chenggu / xingming / lingqian / huangli / liuren / **bazi** / **ziwei**）全部外抛 FortuneError；jiemeng core 是纯静态查表 + null fallback 设计，无业务异常路径，不接入（避免为接入而接入）|
| 21 | 全局错误边界 + 友好态 | §7 | `[x]` | `src/components/common/AppErrorBoundary.vue`（包裹 RouterView，onErrorCaptured + 路由切换自动清错）+ `src/main.ts` 配置 `app.config.errorHandler` 兜底；各模块 page 内的 `compute-error-card` / `xm-error-card` 友好态保留 | 三层防御（业务 page → AppErrorBoundary → app.errorHandler），FortuneError 优先呈现 `userMessage`，否则按 i18n key `errorBoundary.fortuneCode.{code}` 解析；所有错误文案三语完备 |

---

## 2. 跨模块的"基础体验流程"完成度

> 用户在任意模块内的完整流程都依赖通用层。

| 步骤 | 期望体验 | 状态 | 备注 |
|------|---------|------|------|
| 1. 首次访问 | URL `?theme=` `?lang=` 引导主题/语言 | `[x]` | `composables/useUrlBootstrap.ts` |
| 2. 选择主题/语言 | 切换即时生效，刷新保持 | `[x]` | `useTheme` `useLocale` + localStorage |
| 3. 进入业务页 | 自动 lazy load 对应模块 + i18n 子包 | `[~]` | 模块 lazy 已生效；i18n 子包**未拆**（详见 §1#22） |
| 4. 输入生辰 | 各模块表单字段一致 | `[x]` | bazi/ziwei/chenggu 共用 `components/common/BirthForm.vue`（已物理移到 common 目录）；huangli/lingqian/liuren/jiemeng 不涉及生辰；xingming 用名字输入（不同业务输入域） |
| 5. 排盘 | 1.5s 骨架屏 → 渐入 | `[x]` | `useSkeletonReveal` |
| 6. 查看结果 | 主题色编码（吉/中/凶 / 庙旺得利平不陷）| `[x]` | 6 模块已用 `ResultBanner` 共用结果横幅；`level` 颜色编码下沉到各模块主结果组件（lingqian 6 档 / chenggu 5 档 / xingming 5 档 / liuren 3 档，枚举本就不统一），不强行抽通用 `level` prop |
| 7. 分享结果 | 一键导出 PNG + 复制链接 | `[x]` | 8 模块全部接入 `useShareCard`；分享流程：点击"生成分享卡片" → SharePreviewDialog 弹框预览 → 保存 PNG / Web Share；弹框内有"含入参二维码"用于复现，AppFooter 全局有"入口二维码" |
| 8. 跨模块跳转 | 顶栏导航无缝切换 | `[x]` | AppHeader 8 模块导航 |
| 9. 异常发生 | 友好错误态而非白屏 | `[x]` | 三层防御：(1) 业务 page 内 try/catch + 友好 UI；(2) `AppErrorBoundary` 包裹 RouterView 捕获渲染错误；(3) `app.config.errorHandler` 兜底；FortuneError `userMessage` 优先呈现 |
| 10. 退出/再回 | 状态自动还原 | `[x]` | localStorage |

---

## 3. 子能力详情

### 3.1 国际化（i18n）

| 能力 | 状态 | 实现 |
|------|------|------|
| 三语主文件（zh-CN / zh-TW / en） | `[x]` | `locales/{zh-CN,zh-TW,en}.ts` 各约 290 行 |
| 占卜术语词典 | `[x]` | `locales/divination-terms.{lang}.ts` |
| 模块 i18n 按需懒加载 API | `[x]` | `loadModuleLocale(moduleId, lang)` |
| 模块 i18n 子文件实际拆分 | `[x]` | 8 模块（ziwei + bazi + liuren + chenggu + lingqian + xingming + huangli + jiemeng） × 3 语 = 24 个文件全部独立分割并按需加载；主 locale 文件从 1445 行 → 459 行（-68%）；diff 脚本 `scripts/common/diff-i18n-keys.mjs` 验证 8 模块 723 keys 三语完全对齐 |
| `<html lang>` 同步 | `[x]` | `loadLocale` 内已写 `documentElement.lang` |
| 术语词典覆盖度（十神 10 + 纳音 30 + 紫微 14 主星 + 6 吉 6 煞 + 4 化）| `[ ]` | 待与各模块 i18n 协同核对 |
| disclaimer / brand / nav / common / footer 五语 key 对齐 | `[x]` | 三语已对齐 |

### 3.2 主题系统

| 能力 | 状态 | 实现 |
|------|------|------|
| 国风主题（朱砂/古铜/鎏金/宣纸） | `[x]` | `themes/guofeng/` |
| 极简主题（中性灰 + 蓝绿点缀） | `[x]` | `themes/minimal/` |
| 共享 base / contracts / tailwind | `[x]` | `themes/_shared/` |
| CSS 按需 lazy import | `[x]` | `themes/index.ts` 中 `themeLoaders` |
| 主题切换时 `data-theme` attribute | `[x]` | `useTheme` |
| shadcn-vue UI 套件 | `[x]` | `components/ui/`（button/label/radio/select 已引入） |

### 3.3 共享 Composables

| Composable | 状态 | 用途 |
|-----------|------|------|
| `useLocale` | `[x]` | 语言切换 + 模块 i18n 包加载 |
| `useTheme` | `[x]` | 主题切换 + CSS 懒加载 |
| `useUrlBootstrap` | `[x]` | URL 引导主题/语言（写 storage） |
| `useUrlSync` | `[x]` | URL 与 store 双向同步（**仅** theme/lang） |
| `useSkeletonReveal` | `[x]` | 排盘骨架 1.5s 揭示 |
| `useSvgDraw` | `[x]` | 八字/紫微关系线绘制 |
| `useShareCard` | `[x]` | html2canvas 导出 PNG + Toast 反馈（8 模块全部接入） |
| `useBirthStore` | `[x]` | 生辰输入跨模块共享（bazi/ziwei/chenggu 等共用） |
| `useAnnotBar` | `[x]` | 八字四柱标注栏 |
| `useTrueSolarTime` | `[x]` | 真太阳时修正：bazi/ziwei core 入口已接入；BirthForm 暴露开关 + 35 城市经度查表 + 自定义经度（2026-04-27） |

---

## 4. 跨模块约束

| 约束 | 状态 | 说明 |
|------|------|------|
| 所有需要生辰的模块复用同一 BirthInput | `[x]` | `BirthForm.vue` 物理位于 `components/common/`，bazi/ziwei/chenggu 共用；其他 5 模块业务输入域不同（黄历=日期 / 灵签=签号 / 解梦=关键词 / 姓名学=姓名 / 小六壬=即时起卦） |
| 所有 core 函数走 `divinationStore` 缓存（in-memory） | `[~]` | `cachedCompute` helper + 接入指南 JSDoc 已就绪；评估后认定主排盘按钮路径不应短路（保护"重排即重算 + 触发动画"语义），业务侧暂未消费 |
| 所有"按生辰排盘"模块支持**刷新存活**（localStorage 快照） | `[~]` | lingqian（itemId）+ liuren / bazi / ziwei（输入指纹）已落地；chenggu / huangli / jiemeng / xingming 形态不同（即时计算 / 输入即结果），无需快照 |
| 所有结果卡走 `ResultCard` 壳 | `[x]` | 设计文档 §4.4 原意已由 `ResultBanner` 充分承载（6 模块共用结果揭示横幅 + 主题对偶视觉）；各模块的 level 颜色编码（lingqian 6 档 / chenggu 5 档 / xingming 5 档 / liuren 3 档）枚举本就不统一，下沉到各自主结果组件即可，不在通用层做强行统一（2026-04-27 决策） |
| 所有 core 异常外抛 `FortuneError` | `[x]` | `FortuneError` 已定义（`src/lib/errors.ts`）；7 模块（chenggu/xingming/lingqian/huangli/liuren/bazi/ziwei）已外抛；jiemeng core 为纯静态查表设计无异常路径，故不接入 |
| 全局 ErrorBoundary 兜底 | `[x]` | `AppErrorBoundary` 包裹 RouterView + `app.config.errorHandler` 双重兜底 |

---

## 附录 A · 知识库与数据来源

| 资产 | 类型 | 来源 | 状态 |
|------|------|------|------|
| 共享术语对照表（中/繁/英） | B 类 | 公版整理 | `[~]` 已做 disclaimer/nav/common，业务术语需补齐 |
| About 页内容（数据来源 + AI 辅助声明 + 许可声明） | B 类 | 自撰 | `[x]` 4 个法律页（about/disclaimer/data-source/privacy）三语完整，含数据来源、AI 辅助声明、许可声明 |

## 附录 B · 数据工程

- [x] 各模块 i18n 拆到 `locales/modules/${id}.{lang}.ts`（避免主文件膨胀）—— 2026-04-27 完成，8 模块 × 3 语 = 24 文件，主 locale 减重 68%
- [ ] ⏳ 共享术语对照表完善（覆盖所有占卜模块共用术语）
- [x] About 页内容草案（已落地为 about/disclaimer/data-source/privacy 4 页）

## 附录 C · 依赖与构建

| 依赖 | 用途 | 状态 |
|------|------|------|
| `tyme4ts ^1.4.6` | 八字/黄历/紫微辅助 | `[x]` |
| `iztro ^2.5.8` | 紫微排盘 | `[x]` |
| `@vueuse/core / router ^10.11.0` | composable | `[x]` |
| `pinia / vue-router / vue-i18n` | 状态/路由/i18n | `[x]` |
| `tailwindcss ^4.2.2 / shadcn-vue ^2.6.2 / reka-ui ^2.9.6` | UI | `[x]` |
| `unplugin-auto-import / vue-components` | 自动注册 | `[x]` |
| `html2canvas ^1.4.1` | ShareModal PNG 导出 | `[x]` 已装并被 6 模块经 `useShareCard` 使用 |
| `chinese-character-strokes ^0.0.3` | 姓名学 | `[x]` 已装并按需 dynamic import |
| `fuse.js ^7.3.0` | 解梦搜索 | `[x]` 已装并被 jiemeng 模块使用 |
| `tyme4ts ^1.4.6` | 八字/称骨/紫微辅助/黄历/小六壬 | `[x]` |
| `iztro ^2.5.8` | 紫微排盘 | `[x]` |
| `vite-plugin-compression` | 预压缩 | `[ ]` |
| `rollup-plugin-visualizer` | 体积分析 | `[ ]` |

## 附录 D · 工时估算（剩余）

> 2026-04-26 巡检后修订：`FortuneError` / `ShareModal` 实际已实现，从工时表中剔除；剩余项保持原排期。

| 任务 | 工时 | 优先级 | 状态 |
|------|------|--------|------|
| ~~BirthInput 物理移到 `components/common/`~~（已完成） | ~~0.3 天~~ | ~~🟡 中~~ | `[x]` |
| 真太阳时 core 接入（`useTrueSolarTime` 已抽出，缺 BirthForm UI + bazi/ziwei core 调用） | 0.3 天 | 🟢 低（V2） | `[x]`（2026-04-27 完成） |
| ~~`UserState.name` 字段 + setter~~（已完成） | ~~0.1 天~~ | ~~🟢 低~~ | `[x]`（store 字段就位；xingming 模块仍持 module store） |
| ~~全局 ErrorBoundary（App.vue 包裹 + 兜底页）~~（已完成） | ~~0.3 天~~ | ~~🟡 中~~ | `[x]` |
| ~~`utils/element.ts` + `utils/tenGod.ts` 抽离~~（已完成；bazi 重构待二阶段） | ~~0.3 天~~ | ~~🟢 低~~ | `[~]` extract-only |
| ~~`ResultCard` 完整壳（`level` prop + 三槽位）~~（已决策归档 2026-04-27） | ~~0.4 天~~ | ~~🟢 低（V2）~~ | `[x]` 设计意图已由 ResultBanner 充分承载；level 编码下沉模块自治（详见 §1#12 备注） |
| ~~4 个内容路由（about/disclaimer/data-source/privacy）+ 页面~~（已完成） | ~~1.0 天~~ | ~~🔴 高~~ | `[x]` 三语法律级文案 |
| ~~模块 i18n 子文件拆分~~（已完成 2026-04-27） | ~~0.5 天~~ | ~~🟢 低~~ | `[x]` 8 模块 × 3 语 = 24 文件 + diff 脚本推广为 common 通用版 |
| ~~字体栈与 CSS 变量命名一致性核对~~（已完成 2026-04-27 深夜·F 轮） | ~~0.3 天~~ | ~~🟢 低~~ | `[x]` 决策"保留视觉 + 补全 fallback"，contracts.css 写入决策注释 |
| ~~抽屉滑入动效（移动菜单）~~（已完成 2026-04-27 深夜·D 轮） | ~~0.2 天~~ | ~~🟢 低~~ | `[x]` 窄屏 nav popover 走 layout-drawer-slide-in 动画 |
| ~~BirthForm i18n key 重整（`bazi.*` → `common.birthInput.*`）~~（已完成 2026-04-27） | ~~0.3 天~~ | ~~🟢 低（重构期）~~ | `[x]` 6 子树 / 21 keys 已迁；deprecated alias 保留 |
| **剩余合计** | **0 天**（V1 已全部清零，剩余仅 V2/二阶段任务，无新增项） | | |

## 附录 E · 风险与对策

| 风险 | 应对 |
|------|------|
| ~~BirthInput 物理位置仍在 bazi/ 路径下~~（已迁到 `components/common/`） | 已解决 |
| ~~About / Disclaimer 等页缺失影响合规上线~~（4 页已落地，三语完整） | 已解决 |
| ~~模块化 i18n 子目录为空，主文件持续膨胀~~（2026-04-27 全部 8 模块拆分完成） | 已解决 · 主 locale -68% · 24 chunks 按需加载 |
| 新增依赖（html2canvas / fuse.js / 笔画库）首屏体积膨胀 | 全部走 dynamic import，仅用到时按需加载 |
| ~~全局 ErrorBoundary 缺失，意外异常会白屏~~（`AppErrorBoundary` + `app.config.errorHandler` 双重兜底） | 已解决 |
| ~~`BirthForm` i18n key 仍属 `bazi.*` namespace~~（2026-04-27 已迁至 `common.birthInput.*`） | 已解决 |
| `useTrueSolarTime` / `lib/element.ts` / `lib/tenGod.ts` 已抽出但 core 未消费 | extract-only 策略防止现有 bazi 回归；按业务节奏接入 |
