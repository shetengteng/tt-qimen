/**
 * 周公解梦 · 敏感词词典
 *
 * 对应设计文档 §8 / todo T-16.4：
 *   古籍中确实存在"死亡 / 鬼神 / 疾病 / 血光"等象征性意象（如"齿落者主丧事"），
 *   这是中国传统解梦文化的本来面貌，不应机械屏蔽。
 *   本模块的策略是：当词条命中敏感词时，在 UI 底部追加一条更柔和的
 *   "文化象征提示"，引导用户理解这些意象只是传统象征而非命运判词。
 *
 * 只对 `title / classical / keywords` 命中时触发。
 *
 * 后续若要升级，可：
 *   - 扩充词典（与 todo T-16.4 50 词目标靠齐）
 *   - 按敏感度分级（light / strong）、对 strong 级增加顶部 badge
 */

/**
 * 敏感词列表。
 *
 * 设计原则：
 *   - 只收录真正"令人不适"的字/词，如"丧 / 死 / 尸 / 棺 / 血 / 鬼 / 病"
 *   - 不收"蛇 / 虎"等虽然古籍认为凶但日常无歧义的词
 *   - 全中文；不区分繁简（词条正文也只有中文）
 */
export const SENSITIVE_WORDS: readonly string[] = Object.freeze([
  '死',
  '亡',
  '丧',
  '殁',
  '尸',
  '棺',
  '坟',
  '墓',
  '冢',
  '鬼',
  '魂',
  '魄',
  '妖',
  '怪',
  '疾',
  '病',
  '血',
  '伤',
  '凶',
  '灾',
  '祸',
  '忧',
  '哭',
])
