# 10 · 八字命盘模块 · TODO（功能视角）· 95%

> 对应设计文档：[`../modules/10-八字模块.md`](../modules/10-八字模块.md)
> 状态约定：`[x]` 已完成 · `[~]` 部分实现 · `[ ]` 待办
> **本版重写**：2026-04-19 · 改为以"设计文档 §2 模块边界 + §5 交互流程"为骨架
> **最近一次核查**：2026-04-20 · 神煞模块已接入 lunisolar `plugin-char8ex`（见研究文档 [`../bazi/2026-04-20-02-八字神煞现成框架调研.md`](../bazi/2026-04-20-02-八字神煞现成框架调研.md)）；文案数据源调研归档（见 [`../bazi/2026-04-20-01-八字文案数据源调研.md`](../bazi/2026-04-20-01-八字文案数据源调研.md)）；格局识别已落地；UI 层修复国风 padding / 连线起点 / 进入页面自动测算移除；主算法（四柱/十神/藏干/纳音/流年/起运/大运）继续走 tyme4ts，lunisolar 仅服务神煞子模块（曾尝试将主算法迁移到 lunisolar，5 组样本验证 100% 等价但 ROI 不足，已回滚）
> **同步**：2026-04-27 · 巡检发现以下三项早已落地但 todo 文档未及时回写：(1) `data/nayin.ts` 30 段含义已 verified（含 classical / source 古籍出处，对照《三命通会·卷一》），#1/§3.1/§5/§7.B 全部 `[~]` → `[x]`；(2) `FourPillarsTable.vue` 已通过 `nayinAnnotItems + InlineAnnotsBar` 在表格下方就近渲染纳音含义（点击 chip 展开 short + long），不需要单独的 modal；(3) `ShenshaBlock.vue` 已通过分组 `InlineAnnotsBar` 实现"chip → 就近展开 long"，#9 长释义 UI 升级为 `[x]`，仅剩 29 条 pending 项的 long 字段考据补齐（数据工作，非工程）；(4) 真太阳时联动今日完成（详见通用层 todo 2026-04-27 同步项）：bazi `calculateBazi` 入口接入 `useTrueSolarTime(birth)`，#7 `[ ]` → `[x]`
> **同步**：2026-04-27 (深夜·A 轮) · 排盘结果**刷新存活**（P4 · 输入指纹快照）——
>   - 新增 `src/modules/bazi/stores/baziStore.ts`（与 `liurenStore.ts` / `ziweiStore.ts` 对齐）：仅承载 `lastComputed: { calendar, year, month, day, hour, minute, gender, longitude } \| null` 一个字段；显式 `StorageSerializers.object` 以避免 default null 时被推断为 boolean serializer 把对象存成 `"[object Object]"`。
>   - 故意**不序列化 BaziChart 本体**：chart 字段多 + 跨版本反序列化易脏 + 同 birth 重算 < 50 ms（tyme4ts），mount 时重算更安全干净。
>   - `BaziPage.vue`：`onPaipan` 成功后 `recordComputed()`；`onRepaipan` 时 `clearComputed()`；`onMounted` 在无 deeplink query 时调 `tryRestoreLastResult()`，命中即重算 + `skeleton.revealImmediately()` 跳过 1.5s 骨架屏。
>   - 与"useDivinationStore 缓存（in-memory · 路由保活）"是**正交方案**：本轮解决"刷新页面静默恢复上次盘"，不是 CPU 短路；§3.5 性能表的 `useDivinationStore` 仍维持"按钮路径不接入"的判断（评估理由见 #6 行）。
>   - 验证：ReadLints 0；vite build 4.24s；新 `BaziPage` 95.74 → 96.00 KB（+0.26 KB，acceptable）；`scripts/common/probe-snapshot-restore.mjs` 13/13 contract case（同盘/字段穷举/longitude 三段切换/损坏快照/null 全过）。
> **同步**：2026-04-27 (深夜·X 轮) · 文档失修体检：(1) §2 第 11 行"一键分享" `[ ]` → `[x]`：`useShareCard` composable 已接入 `BaziPage.vue`（line 22 import + line 218 实例化 + `share-card-frame` ref + buildShareUrl 复制链接 + ShareToast 提示），#11 早已落地；(2) §63 一键分享 `[ ]` → `[x]` 同步回写。
> **同步**：2026-04-27 (深夜·I 轮) · **i18n 模块化拆分**：bazi 三语 i18n 块（zh-CN 144 行 / zh-TW 144 行 / en 144 行 + collapse 子树死代码清理后 = 144 keys × 3 语）从主 locale 文件抽离至 `src/locales/modules/bazi.{zh-CN,zh-TW,en}.ts`，主 bundle 体积下降；`router.beforeEach` `loadModuleLocale('bazi', lang)` 已就绪，进入 `/bazi` 时按需 lazy-load 8.5 KB（zh-CN 7.7K / zh-TW 7.7K / en 9.1K）；附带连带修复 `CollapsibleSection.vue` 反向依赖 `bazi.collapse.*` → `common.collapse.*`，bazi 模块此项 i18n 反向依赖现归零；`scripts/common/diff-i18n-keys.mjs` 验证三语 144 keys 完全对齐。
> **同步**：2026-04-27 (M3·流年扩量) · **流年模板 30 → 300 段（60 甲子 × 5 旺衰）**：
>   - 设计目标 §6 长期遗留项（设计文档 §6 早已写明"60 甲子 × 5 旺衰 = 300 段"，但实现一直停留在 v1 30 段 `十神 × 吉凶`），本轮规则化生成补齐。
>   - 数据：`src/modules/bazi/data/flowYearHints.ts` 增加 `FLOW_YEAR_HINTS_V2: Record<string, FlowYearHint>`（300 段）+ `getFlowYearHintV2(ganzhi, strength)` 辅助 lookup；保留 `FLOW_YEAR_HINTS`（30 段）+ `getFlowYearHint(tenGod, tendency)` 作为类型完备性 fallback。
>   - 生成：`scripts/bazi/build-flowyear-hints-v2.mjs`，规则参考《三命通会·卷九/卷十一·论行年》《五行精纪·流年》纲要——
>       · 干支气势 6 类：同气 / 干生支 / 支生干 / 干克支 / 支克干 / 二气并行；
>       · 旺衰对策 5 档：极旺忌再生扶喜泄秀 → 偏旺稍泄稍制 → 中和平稳 → 偏弱印比生扶 → 极弱必须强援忌冲克；
>       · 文笔："宜/防/可/利"等缓和措辞，避免"必/绝对"。
>   - 接入：`core/bazi.ts → buildFlowYears` 接入新参数 `dayMasterStrength: StrengthLevel`，主流程优先查 v2、未命中回退 v1（验证显示 v2 100% 命中，v1 仅作类型完备性保险）。
>   - 验证：`scripts/bazi/verify-flowyear-v2-coverage.mjs` 300/300 全枚举覆盖；`scripts/bazi/verify-flowyear-builder.mjs` 5 命盘 50/50 v2 命中、跨"偏旺/极旺"两档旺衰；type-check 0 新增报错；vite build 通过（`engine-bazi` bundle +60kB，可接受）。
>   - 本质：把 §6 风险条"工程量爆"从"分批 LLM 生成 + 抽样审校"改为"规则化生成 + 后续人工微调极端样本"，工程量从 2 天 → 0.5 天。
>   - 进度：78% → 84%（300 段流年大头落地，剩余 M3 项：大运 100 段 + 神煞 long 29 条）。
> **同步**：2026-04-28 (B-1 / B-3 推进) · **神煞 long 17 项考据补齐 + 模板按需懒加载（manualChunks 拆 data-bazi 独立 chunk）** ——
>   - **B-1 神煞 long 17 项升级 verified**：从 ST3 古籍交叉考证 29 条 pending，按"古籍可溯源"原则严格分类后，补齐 17 项可靠源点（ST3 · L171/L177/L195-199/L215/L295-297/L299-301/L305/L307-309/L311-315/L357/L359/L361/L369）。新升级条目：天印貴人 / 天德合 / 月德合 / 文星貴人 / 帝座 / 九醜 / 八專 / 六厄 / 十惡大敗 / 地網 / 天羅 / 孤鸞 / 紅艷 / 隔角 / 陰陽煞 / 月煞 / 絞煞，全部含 short + long ≤80 字 + classical 古籍引文 + source 行号锚点。
>   - **保留 12 项 pending**（明确判定为"无单立专章 / 散见无独立条"）：天廚貴人 / 天厨貴人 / 垣城 / 日德 / 日貴 / 德 / 秀 / 紅鸞 / 墓煞 / 太白星 / 時墓 / 血刃。这些是天乙系散见配神 + 古籍未独立列出的衍生煞，凡 ST3/WJ/YZ 三本主要古籍中无可信引文的，统一保持 pending 不做 LLM 编造，避免污染数据集"古籍可溯源"的核心定位。
>   - **统计变化**：`SHENSHA_MEANING_COUNT` 从 43 verified / 29 pending → **60 verified / 12 pending / 72 total**；`extracted/05-shensha.md` 同步标注 T-2.4-B 推进段。
>   - **B-3 模板懒加载（产品决策走向）**：调研后发现紫微 lazy.ts 范式（多 locale × 大资产 × view 异步消费）不适用于八字单 locale 场景；改为更精准的"manualChunks 拆 data-bazi 独立 chunk"方案——
>       · `vite.config.ts` `manualChunks` 从 object 改为 function (id)，新增 `if (id.includes('/src/modules/bazi/data/')) return 'data-bazi'` 切分规则；
>       · 效果对照：BaziPage 226 KB → **61 KB (-73%)** / gzip 59 KB → **19 KB (-68%)**；data-bazi (新增) 74 KB / gzip 37 KB；
>       · 长期 HTTP 缓存收益：data-bazi 是纯数据，组件代码迭代时浏览器缓存命中，避免重新下载 37 KB gzip；
>       · core sync 函数无需改 async，零业务侧改动。
>   - 验证：ReadLints 0；vue-tsc 0 新错；vite build 5.92s 通过；4 个 bazi verify 探针（flowyear-v2 300/300、fortune-v2 100/100、flowyear builder 50/50 v2 命中、decade builder 5/5 命盘 8/8 段唯一）+ 3 个 ziwei probe（boundaries 35 + fortune-error 26 + minorStars 144 三语 0 mismatch）+ snapshot-restore 13/13 + birthform 6/6 全 PASS；diff-i18n-keys 8 模块 723 keys 三语对齐。
>   - **进度**：90% → **92%**（M3 神煞数据工程闭环 ↑ 17 项 + 性能拆分长期收益落地）。
>
> **同步**：2026-04-28 · **性能 / 缓存 / 文档双向澄清** ——
>   - **bazi 排盘耗时实测**（`scripts/common/probe-perf-bazi-ziwei.mjs`，5 样本 × 50 次 trim mean）：trim mean **1.26ms** / p95 2.09ms / max 5.83ms。**强化既有判断**：bazi 不接入 useDivinationStore 缓存（按钮 = 重算的语义不破坏）+ 不需要 Web Worker（远低于 100ms 阈值）。§3.5 性能表 #176 / #178 两行 `[ ]` 升级为 `[x · 不接入 (实测 1.26ms)]` / `[x · 不需要 (实测 1.26ms)]`，附录 C 工时表"性能 · 接入 useDivinationStore 缓存" / "性能 · Web Worker 拆分（如需）"两条标 `[x · 已决定不接入]`。
>   - **数据扩量补回归写**：发现 §3.4 流年模板颗粒度升级 / §3.5 资产表 / 附录 B / 附录 C 4 处 `[ ]` 与顶部 2026-04-27 (M3·流年扩量) 同步段事实不一致——**2026-04-27 早已落地 v2 300 段 + builder 优先 v2 命中**，本轮统一回写 `[x]` + 同步指向 `scripts/bazi/build-flowyear-hints-v2.mjs`。
>   - 文档自检：`rg "FLOW_YEAR_HINTS_V2|getFlowYearHintV2" src/modules/bazi/` 命中 4 处（data/flowYearHints.ts × 3 + core/bazi.ts × 1）；`scripts/bazi/verify-flowyear-v2-coverage.mjs` 300/300 PASS、`scripts/bazi/verify-flowyear-builder.mjs` 5 命盘 50/50 v2 命中。
>   - **进度**：88% → **90%**（M3 数据工程 100% 闭环 + 性能尾巴决策清晰，仅剩神煞 long 29 条考据为低优数据工作）。
>
> **同步**：2026-04-28 (B-4 验证 + G-5 lib 消费一次性收尾) · **真实剩余项一次性归档** ——
>   - **B-4「11 格局 200 字长解读」实际状态：100% 完成（false positive in TODO）**：
>       · 排查 `src/modules/bazi/data/patternLongInterpret.ts` 发现 11 格局长解读已全部 verified（180-220 字 / 含 classical 古籍引文 + source 行号锚点 / `PATTERN_LONG_INTERPRET_STATUS = 'verified'`）；
>       · `InterpretBlock.vue` 已正确 `import { getPatternLongEntry, getPatternLongInterpret }` 并通过可折叠区域渲染长解读 + 古籍出处；
>       · 顺手修复**B-4 副产物**：`InterpretBlock.vue` 第 115 / 159 行硬编码中文「`{{ expanded ? '收起' : '查看' }}{{ patternName }}完整解读`」改为 i18n key `bazi.interpret.patternExpand.{open,close}`，三语 locale 文案对齐落地（en 占位的"Show / Hide full {name} interpretation"等待未来 patternLong 英文版补齐时即可使用）；
>       · 注：英文 `getPatternLongInterpret` 在 `if (locale.value === 'en') return null` 处直接降级，意味着英文界面 patternLong 不展示——属于设计抉择（专业级长解读暂未译稿），不在 B-4 工作范围；UI 已为未来准备好 i18n key 化的 toggle 文案。
>   - **G-5「让 bazi 真正消费 lib/element + lib/tenGod」**（消除双源 drift 风险）：
>       · `bazi/types/index.ts` `ElementName` / `TenGodType` 改为从 `@/lib/element` / `@/lib/tenGod` re-export，外部模块 `import type { ElementName, TenGodType } from '../types'` 路径不感知重构；
>       · `bazi/core/bazi.ts` 第 83/86 行 `ELEMENT_NAMES` / `ELEMENT_SCORE_KEY` 私有定义删除，改为从 `@/lib/element` 导入；
>       · `bazi/core/pattern.ts` `YANG_STEMS` / `isYang()` / `STEM_ELEMENT` / `relation()` / 完整版 `stemTenGod()` 私有副本共 76 行全部下线，改为消费 `lib/tenGod.calcTenGod()` —— 仅保留 `stemTenGod()` 函数名作 module-local 适配层（处理 calcTenGod 的 `null` fallback → 中立值"比肩"）；
>       · `bazi/components/ElementsRadar.vue` `ELEMENT_KEY` 私有 map 删除，改为从 `lib/element.ELEMENT_PINYIN` 导入；`RADAR_ORDER` 改为消费 `lib/element.ELEMENT_NAMES`；
>       · 验证：bazi 探针 trim mean 重构前 1.87ms / 重构后 3.07ms（仍远低于 100ms Web Worker 阈值）；vite build 6.55s PASS；vue-tsc 0 新错；ReadLints 0；附录 C / 附录 D 「bazi 真正消费 lib/element + lib/tenGod」从 `[~]` 升级为 `[x]`。
>   - **进度**：92% → **95%**（B-4 false positive 归档 + G-5 双源 drift 风险消除 + i18n key 一致性顺手修复；剩余仅 B-1 同名神煞跨柱去重 / B-2 神煞 long 12 条 pending（明确无单立古籍专章无法考据）2 项数据/产品决策类工作，可视为 MVP V1 生产就绪）。

> **同步**：2026-04-27 (M3·大运扩量) · **大运模板 30 → 100 段（每段 3 变体）= 300 句**：
>   - 设计目标 §6 长期遗留项（设计文档 §6 写明"100 段含 3 措辞变体"，但实现长期停在 v1 30 段单变体），本轮规则化生成补齐。
>   - schema：`10 十神 × 5 旺衰 × 2 取向（顺势/逆势）= 100 段`，每段 3 句变体（开篇/中段/结句不同写法）；`DecadeStance` 由 `(tenGod, strength)` 推导——身强时克泄组（食伤财官杀）顺势、扶身组（印比）逆势；身弱时反之；中和时默认顺势。
>   - 数据：`src/modules/bazi/data/fortuneHints.ts` 增加 `DECADE_HINTS_V2: Record<string, DecadeHintV2>`（100 段，每段 `{ variants: [3], tags: [3] }`）+ `judgeDecadeStance(tenGod, strength)` + `getDecadeHintV2(tenGod, strength, stance, variantIdx)`；保留 v1 `DECADE_HINTS` + `getDecadeHint(tenGod, tendency)` 作为类型完备性 fallback。
>   - 生成：`scripts/bazi/build-fortune-hints-v2.mjs`，规则参考《滴天髓·岁运》《三命通会·卷四·论行运》纲要——
>       · 十神意象 10 类：比/劫=同辈、食/伤=才艺表达、正/偏财=收入机遇、正/七官杀=贵人威权、正/偏印=学业灵感；
>       · 旺衰 × 取向 5 档策略：极旺顺势"乘势开拓 留余地"、极旺逆势"修身养性 避锋芒"、偏旺顺势"乘势开拓"、偏旺逆势"修身避争"、中和顺势"稳健精进"、中和逆势"调息调身"、偏弱顺势"借力推进"、偏弱逆势"宜守不宜攻"、极弱顺势"专研复原"、极弱逆势"保命守身"；
>       · 3 变体保证同一段 hint 在不同大运段不重复（按 i mod 3 旋转）。
>   - 接入：`core/bazi.ts → buildDecades` 接入新参数 `strength: StrengthLevel`，主流程：`stance = judgeDecadeStance(tenGod, strength)` → `v2 = getDecadeHintV2(tenGod, strength, stance, i)` → 命中用 v2、未命中回退 v1。
>   - UI：`DayunTimeline.vue` 完全不需改——`hint` 仍是单字符串字段，v2 在 builder 内已抽取一句变体写回 `hint`。
>   - 验证：`scripts/bazi/verify-fortune-v2-coverage.mjs` 100/100 全枚举覆盖；`scripts/bazi/verify-decade-builder.mjs` 5 命盘（覆盖偏旺/偏弱/中和三档）每盘 8/8 段 hint 全唯一、v2 句式 100% 命中；type-check 0 新增报错；vite build 通过（`engine-bazi` 287kB → 309.72kB，+22kB 可接受）。
>   - 进度：84% → 88%（M3 完成 2/3，剩余神煞 long 29 条）。

---

## 0. 模块定位

`tt-qimen` 的**核心专业支柱**之一（与紫微并列"命理双擎"）。
- 对标专业算命站，让用户看到完整的四柱 + 大运时间轴 + 流年标签。
- 所有解读基于"模板化组合"的规则生成，避免 LLM 自由发挥的幻觉。
- MVP 范围：命盘 + 五行 + 十神 + 大运（10 年一运）+ 流年（10 年）。

---

## 1. 功能边界与完成度（核心）

> 来自设计文档 §2「MVP 做 / 不做」表。这是用户能直接感知到的"功能开关"。

| # | 功能维度 | MVP 范围 | 状态 | 实现位置 | 缺口 / 备注 |
|---|---------|---------|------|---------|------------|
| 1 | **四柱（年/月/日/时）** | 天干 + 地支 + 纳音 + 十神 | `[x]` | `core/bazi.ts → toPillarInfo` (tyme4ts) + `components/FourPillarsTable.vue` + `data/nayin.ts` | 纳音名称 + 30 段含义（verified · 古籍出处《三命通会·卷一》）+ FourPillarsTable 用 `nayinAnnotItems + InlineAnnotsBar` 就近展开 |
| 2 | **五行分布** | 雷达图 + 日主旺衰 + 喜用神 | `[x]` | `core/bazi.ts → calculateElements / judgeStrength / deriveFavorable` + `ElementsRadar.vue` | 完整：本气 0.7 / 中气 0.2 / 余气 0.1 + 月令×1.5 |
| 3 | **十神结构** | 年/月/时三干十神含义 | `[x]` | `core/bazi.ts → tenGods` + `data/tenGods.ts`（10 段含义） + `ShishenStructure.vue` | — |
| 4 | **命盘简析** | 模板化 ~150 字总结 | `[x]` | `core/bazi.ts → buildInterpret` + `data/interpretTemplate.ts` + `InterpretBlock.vue` | 双段 ~150 字 + 4 个 chip |
| 5 | **大运** | 8-10 段 × 干支 × 十神 × 吉中凶 + 一句话 | `[x]` | `core/bazi.ts → buildDecades`（tyme4ts `ChildLimit.getStartDecadeFortune()`）+ `data/fortuneHints.ts`（**v2 主表 100 段 × 3 变体 = 300 句 + v1 30 段 fallback**）+ `DayunTimeline.vue` | 2026-04-27 数据扩量到 100 段（10 十神 × 5 旺衰 × 2 取向），每段 3 措辞变体；规则化生成（`scripts/bazi/build-fortune-hints-v2.mjs`），覆盖 100%；5 命盘 40/40 段 hint 全唯一、v2 句式 100% 命中 |
| 6 | **流年** | 当年 + 后续 10 年 × 干支 × 五行 × 一句话 | `[x]` | `core/bazi.ts → buildFlowYears`（tyme4ts `SixtyCycleYear`）+ `data/flowYearHints.ts`（**v2 主表 300 段 + v1 30 段 fallback**）+ `FlowYears.vue` | 2026-04-27 数据扩量到 300 段（60 甲子 × 5 旺衰）；规则化生成（`scripts/bazi/build-flowyear-hints-v2.mjs`），覆盖 100%；5 命盘 50/50 v2 命中、跨"偏旺/极旺"两档旺衰 |
| 7 | **真太阳时** | 可选开关 | `[x]` | `core/bazi.ts → calculateBazi` 入口接入 `useTrueSolarTime(birth)`（2026-04-27） + `BirthForm.vue` 高级选项块 | longitude undefined → noop 兼容；UI 暴露开关 + 35 城市下拉 + 自定义经度 + 实时偏移分钟数预览 |
| 8 | **格局识别**（设计文档 §3.2） | 11 个常见格局 | `[x]` | `core/pattern.ts → detectPattern`（11 格局 + 透干判定 + 杂气库分支）+ `core/bazi.ts` 集成 → `BaziChart.pattern` + `InterpretBlock` 第二段使用 | `PATTERN_COMMENT` 提供一句话点评；后续可扩展每格局 ~200 字长解读 |
| 9 | **神煞**（设计文档 §11 后续） | 8-12 常用神煞 | `[x]` | `core/shensha.ts → detectShensha`（lunisolar `plugin-char8ex`）+ `data/shenshaMeaning.ts`（72 条 · **60 verified + 12 pending**，2026-04-28 T-2.4-B 推进）+ `components/ShenshaBlock.vue` + `InlineAnnotsBar`（chip 点击就近展开 long） + 双主题样式 + i18n 三语 | 算法覆盖 plugin 内置 72 神煞（远超 MVP 8-12）；72 条短释义全覆盖、60 条已含 long + 古籍出处（ST2/3/6 + WJ + YZ）；剩余 12 条 pending 已明确判定"无单立专章"，按古法标准不做 LLM 编造（详见 `2026-04-22-03-八字文案溯源改造方案TODO.md` § 2.4 P2 + `extracted/05-shensha.md` T-2.4-B 推进段） |
| 10 | **流月/流日**（设计文档 §11 后续） | — | `[ ]` | — | 不属于 MVP；扩展项 |
| 11 | **分面解读**（事业/感情/财富/健康，设计文档 §11 后续） | — | `[ ]` | — | 不属于 MVP；扩展项 |

---

## 2. 交互流程完成度（设计文档 §5 / 原型图 §4.2）

> 用户从打开页面到看到结果的完整路径。每一步的功能完整度。

| 步骤 | 期望体验 | 状态 | 备注 |
|------|---------|------|------|
| 1. 进入 `/bazi` | 路由懒加载页面 | `[x]` | `router/index.ts` 已注册 |
| 2. 看到生辰输入表单 | 公历/农历切换 + 年月日时辰 + 性别 + 真太阳时开关 | `[x]` | 真太阳时高级选项块（开关 + 城市下拉 + 自定义经度）已就位（2026-04-27） |
| 3. 提交"开始排盘" | 1.5s 骨架屏 → 渐入 | `[x]` | `useSkeletonReveal` |
| 4. 元信息（公农历 / 性别 / 命盘） | 完整显示 | `[x]` | `core/bazi.ts → buildMeta` |
| 5. 四柱表 | 4 列等宽：年月日时 × 干支/十神/纳音 | `[x]` | `FourPillarsTable.vue` |
| 6. 五行雷达 | 0 → 实际值的增长动画 | `[x]` | `ElementsRadar.vue` Vue transition |
| 7. 十神结构 | 默认展开年月时三行，可点开看含义 | `[x]` | `ShishenStructure.vue` + `CollapsibleSection` |
| 8. 命盘简析 | ~150 字模板总结 + 4 chip | `[x]` | `InterpretBlock.vue` |
| 8.5. 神煞 | 吉/中/凶 三组 + 柱位 badge + chip 释义 | `[x]` | `ShenshaBlock.vue` + `core/shensha.ts`（lunisolar plugin-char8ex），双主题 + 折叠段 |
| 9. 大运时间轴 | 桌面 5×2 蛇形网格 / 移动 2×5 蛇形 + 虚线连接 + 标当前 | `[x]` | `DayunTimeline.vue` |
| 10. 流年卡 | 默认当年 + 下拉看未来 10 年 | `[x]` | `FlowYears.vue` |
| 11. 一键分享 | 导出 PNG + 复制链接 | `[x]` | `useShareCard` 注入 + `share-card-frame` 引用 + buildShareUrl 复制链接；toast 提示已统一 |
| 12. 重新排盘 | 清空结果回到表单 | `[x]` | `BirthForm @reset` |

---

## 3. 子功能详情

### 3.1 四柱排盘

| 能力 | 状态 | 备注 |
|------|------|------|
| 公历输入 → SolarTime → EightChar | `[x]` | `core/bazi.ts → toSolarTime` |
| 农历输入 → LunarHour → EightChar | `[x]` | 闰月由 tyme4ts 处理 |
| 早子/晚子换日 | `[x]` | 跟随 tyme4ts 默认规则（设计文档 §10 风险有述） |
| 干支天干阴阳 + 五行 | `[x]` | `core/bazi.ts → toPillarInfo` |
| 纳音名称（白蜡金 / 海中金 ...） | `[x]` | `pillarFromSixtyCycle` 内 |
| 纳音含义文案（30 段） | `[x]` | `data/nayin.ts` 30 段 verified · 含 short / long / classical / source 四字段（《三命通会·卷一》），UI 通过 `InlineAnnotsBar` 在 FourPillarsTable 下就近展开 |
| 藏干表（本气/中气/余气 + 权重） | `[x]` | `core/bazi.ts → buildHideStems` |

### 3.2 五行强弱与喜忌

| 能力 | 状态 | 备注 |
|------|------|------|
| 5 个五行权重求和（含月令×1.5） | `[x]` | `calculateElements` |
| 旺衰 5 档（极旺/偏旺/中和/偏弱/极弱） | `[x]` | `judgeStrength` |
| 喜用神/忌神（弱→印比 / 旺→官杀食伤 / 中和→平衡） | `[x]` | `deriveFavorable` |
| `ElementCell.status: strong/balanced/weak` | `[x]` | UI 雷达图色编码 |

### 3.3 大运

| 能力 | 状态 | 备注 |
|------|------|------|
| 起运年月计算（`ChildLimit`） | `[x]` | `core/bazi.ts → buildDecades` |
| 8 段大运（0-80 岁覆盖）+ 标记当前 | `[x]` | — |
| 每段：干支/五行/十神/吉凶/一句话 | `[x]` | — |
| **模板深度**：每段 3 个措辞变体（设计文档目标 100 段） | `[x]` | 2026-04-27 完成 100 段 × 3 变体（10 十神 × 5 旺衰 × 2 取向）；规则化生成 `scripts/bazi/build-fortune-hints-v2.mjs`，验证 5 命盘 40/40 段全唯一 |

### 3.4 流年

| 能力 | 状态 | 备注 |
|------|------|------|
| 当年 + 后 9 年共 10 段 | `[x]` | `buildFlowYears` |
| 每段：干支/五行/十神/吉凶/一句话 + tag chip | `[x]` | — |
| **模板颗粒度升级**：从"十神×吉凶"→"60 甲子 × 5 旺衰"= 300 段 | `[x]` | 2026-04-27 完成（v2 300 段 + builder 优先 v2 + 5 命盘 50/50 v2 命中、跨偏旺/极旺两档；详见顶部 M3·流年扩量同步项） |
| 默认只显示当年，展开看后续（避免挤爆） | `[x]` | `FlowYears.vue` 折叠交互 |

### 3.5 解读模板（设计文档 §5）

| 资产 | 设计目标 | 当前 | 缺口 |
|------|---------|------|------|
| 大运模板 `fortuneHints.ts` | 100 段（10 十神 × 5 旺衰 × 2 取向 × 3 变体 = 300 句） | 2026-04-27 完成 | 规则化生成（`scripts/bazi/build-fortune-hints-v2.mjs`），验证 5 命盘 40/40 段全唯一 |
| 流年模板 `flowYearHints.ts` | 300 段（60 甲子 × 5 旺衰） | **300 段（v2 主表）+ 30 段（v1 fallback）** | 2026-04-27 完成（规则化生成 `scripts/bazi/build-flowyear-hints-v2.mjs` 300/300 全枚举覆盖；主流程优先 v2 命中、未命中回退 v1，5 命盘 50/50 v2 命中） |
| 十神含义 `tenGods.ts` | 10 段，每段 ~200 字 | 10 段（短描述 + descMn） | 已达到 |
| 纳音含义 `nayin.ts` | 30 段，每段 ~50 字 | **缺** | 需 0.5 天 B 类公版整理 |
| 命盘简析模板 `interpretTemplate.ts` | 150 字模板 + 变量填充 | 已落地（双段+chip） | — |
| 格局点评 `pattern.ts → PATTERN_COMMENT` | 11 格局 × 一句话 | 已落地（内联于 `core/pattern.ts`） | 可扩展为每格局 ~200 字长解读模板 |
| 神煞短释义 `data/shenshaMeaning.ts` | 50+ 神煞 × 短句（≤20 字）+ 长句（≤80 字，预留） | 40+ 短句已落地；long 字段预留未渲染 | 补齐 plugin 全集剩余 ~10 条；长释义统一审校后写入 |

### 3.6 格局识别

| 能力 | 状态 | 备注 |
|------|------|------|
| 月支本气十神 → 8 正格映射 | `[x]` | `core/pattern.ts → TENGOD_TO_PATTERN` |
| 建禄 / 月刃分支（日主同类 + 阴阳判断） | `[x]` | — |
| 透干判定（year/month/hour 天干十神是否 == 候选十神） | `[x]` | `through` 字段输出透出位置 |
| 杂气库（辰戌丑未 + 本气未透）退化为"杂气格" | `[x]` | — |
| 格局名 + `through` + 一句话点评写入 `BaziChart.pattern` | `[x]` | `InterpretBlock` 第二段消费 `pattern.name` |
| 每格局 ~200 字长解读 | `[x]` | `data/patternLongInterpret.ts` 11 格局 verified（180-220 字 + classical 古籍引文 + source 行号锚点）；`InterpretBlock.vue` 通过可折叠区域渲染长解读（i18n key `bazi.interpret.patternExpand.{open,close}` 三语对齐） |

### 3.7 神煞

| 能力 | 状态 | 备注 |
|------|------|------|
| 神煞计算引擎接入（lunisolar `plugin-char8ex`） | `[x]` | `core/shensha.ts → detectShensha`；plugin 内置 50+ 神煞规则，覆盖远超 MVP 8-12 |
| 繁→简字符转换（`chinese-conv`） | `[x]` | plugin 输出繁体 key，UI 统一显示简体 |
| `BaziChart.shensha: ShenshaHit[]`（吉/中/凶 + 柱位 + 短释义） | `[x]` | `types/index.ts` 类型契约 |
| 短释义表（`data/shenshaMeaning.ts`） | `[x]` | 72 条 short 全覆盖（plugin 全集），未命中走兜底（实际已无未命中）；**60 条 long verified**（2026-04-28 T-2.4-B 新增 17 项 · ST3 行号锚点） |
| 失败降级（plugin 异常不影响主链路） | `[x]` | try/catch + 空数组 + UI empty 态 |
| `ShenshaBlock.vue`（吉/中/凶 三组 + 柱位 badge + chip） | `[x]` | 双主题（国风/简约） |
| 折叠段集成到 `BaziPage.vue`（命盘简析 → 神煞 → 大运） | `[x]` | i18n: `bazi.collapse.sectionShensha(Mn)` |
| i18n（zh-CN / zh-TW / en）`bazi.shensha.*` | `[x]` | 标题、分类、柱位缩写、空态、底注 |
| 长释义（80 字内）UI（modal/详情卡） | `[x]` | `InlineAnnotsBar` 实现 chip → 就近展开 long（按吉/中/凶分组容器，避免越组）；43 条 verified 已含 long + 古籍出处，29 条 pending 仅占位 short（数据补全见附录 B） |
| 同名神煞跨柱去重 / 显示策略（如 `天乙贵人(年+月)`） | `[ ]` | 当前每柱独立 chip；可选优化 |

### 3.8 性能

| 能力 | 状态 | 备注 |
|------|------|------|
| 计算结果走 `useDivinationStore` 缓存（in-memory · 路由保活） | `[x · 已决定不接入]` | 2026-04-28 实测 trim mean **1.26ms** / p95 2.09ms（`scripts/common/probe-perf-bazi-ziwei.mjs`），按钮 = 重算 + 触发动画的 UX 语义不应被缓存短路；预留给"扫码 hydrate / 跨路由保活"等场景（与 store 注释设计一致） |
| **刷新页面静默恢复上次盘**（localStorage · `lastComputed` 输入指纹） | `[x]` | 2026-04-27 深夜·A 轮：`stores/baziStore.ts` + `BaziPage.vue` `tryRestoreLastResult` + `recordComputed/clearComputed`；与小六壬 / 紫微对齐；详见顶部 2026-04-27 同步项 |
| Web Worker 排盘（防主线程卡） | `[x · 不需要]` | 2026-04-28 实测 trim mean **1.26ms** / max 5.83ms（`scripts/common/probe-perf-bazi-ziwei.mjs` 5 样本 × 50 次），远低于 100ms 阈值，主线程瞬时阻塞无感；不上 worker |
| 模板按需懒加载 | `[x]` | 2026-04-28 通过 manualChunks 拆 `data-bazi` 独立 chunk 等价完成（紫微 lazy.ts 范式不适用八字单 locale）：BaziPage 226→61 KB (-73%) + data-bazi 74 KB 独立缓存 · 长期 HTTP 缓存收益 |

---

## 4. 跨模块依赖

| 依赖项 | 状态 | 说明 |
|--------|------|------|
| `BirthInput`（共享生辰组件）| `[x]` | `BirthForm.vue` 已物理移到 `src/components/common/` + `useBirthStore` provide/inject 解耦；2026-04-27 同时增加真太阳时高级选项块 |
| `useDivinationStore` | `[~]` | 通用层 helper 就绪（`cachedCompute`），bazi 评估后认为单次 < 50ms 收益不足，主排盘按钮路径不接入 |
| `ShareModal` | `[x]` | bazi 通过 `useShareCard` + `SharePreviewDialog` 接入完成（同 8 模块统一方案）|
| `FortuneError` | `[x]` | `core/bazi.ts → calculateBazi` 已 wrap tyme4ts RangeError → `FortuneError(module='bazi', code='invalid-input')`，cause 保留 |

---

## 附录 A · 知识库与数据来源（设计文档 §8）

| 层 | 类型 | 来源 | 状态 |
|----|------|------|------|
| 主算法（四柱/十神/藏干/纳音/流年/起运/大运） | A 类 | `tyme4ts`（MIT） | `[x]` `core/bazi.ts` |
| 格局识别算法 | A 类 | 公版八字教材（月令取格 + 透干 + 建禄/月刃 + 杂气） | `[x]` `core/pattern.ts` |
| 神煞计算算法 | A 类 | `lunisolar` + `@lunisolar/plugin-char8ex`（MIT，50+ 神煞规则） | `[x]` `core/shensha.ts`（经 `core/lunisolarHelper.ts` 适配 BirthInput→Char8Ex）|
| 繁简转换 | A 类 | `chinese-conv`（MIT） | `[x]` `core/shensha.ts`（神煞 key 繁→简） |
| 十神含义 | B 类 | 公版文献整理 10 段 | `[x]` |
| 纳音含义 | B 类 | 公版文献整理 30 段（`data/nayin.ts` · 含 classical / source 古籍出处） | `[x]` 30/30 verified |
| 格局点评（11 格局一句话）| B 类 | 公版文献整理 | `[x]` `PATTERN_COMMENT`（内联于 `pattern.ts`）|
| 神煞短释义（≤20 字 × 72） | B 类 | 公版文献整理 + 自撰精炼 | `[x]` 72/72（`data/shenshaMeaning.ts`） |
| 神煞长释义（≤80 字 × 60，按古法标准 12 项 pending 无专章） | B 类 | 公版文献整理 + 自撰精炼 | `[x]` **60/72 verified · 12/72 pending（无单立专章）**：2026-04-28 T-2.4-B 推进——ST3 古籍交叉考证 + 17 项 verified 升级（天印贵人 / 天德合 / 月德合 / 文星贵人 / 帝座 / 九醜 / 八專 / 六厄 / 十惡大敗 / 地網 / 天羅 / 孤鸞 / 紅艷 / 隔角 / 陰陽煞 / 月煞 / 絞煞），全部含 ST2/3/6 + WJ + YZ 行号锚点；剩余 12 条 pending 已明确判定为"无单立专章 / 散见无独立条"（天廚貴人 等天乙系散见配神 + 墓煞 等无独立条衍生煞），不做 LLM 编造 |
| 大运吉/中/凶模板 | C 类 | 规则化生成 100 段 × 3 变体（10 十神 × 5 旺衰 × 2 取向） | `[x]` 100/100（300 句） |
| 流年模板 | C 类 | 规则化生成 300 段 + 后续人工修饰极端样本 | `[x]` 300/300（v2 主表，2026-04-27 落地）；v1 30 段保留为 fallback；后续可对"极旺/极弱 + 同气专"等极端组合追加人工微调 |
| 命盘简析模板 | C 类 | LLM 生成 150 字模板 + 变量填充 | `[x]` |

## 附录 B · 数据工程（剩余）

- [x] ✅ `data/nayin.ts` — 30 段纳音含义（B 类）·verified · 含 classical / source 古籍出处（《三命通会·卷一》）
- [x] ✅ `data/fortuneHints.ts` 扩展为 100 段（每段 3 变体）→ 2026-04-27 完成
- [x] ✅ `data/flowYearHints.ts` 重构为 300 段（60 甲子 × 5 旺衰）→ 2026-04-27 完成（规则化生成 0.5 天 / 原估 2 天）
- [x] ✅ `data/shenshaMeaning.ts`（神煞 72 条 short 全覆盖，**60/72 verified**，2026-04-28 T-2.4-B 推进 17 项；剩余 12 条 pending 已确认无单立专章按古法标准保留 pending、不做编造，详见 `2026-04-22-03-八字文案溯源改造方案TODO.md` § 2.4 + `extracted/05-shensha.md` T-2.4-B 推进段）
- [x] ✅ `data/patternTemplates.ts`（11 格局名 + 一句话）→ 已在 `core/pattern.ts → PATTERN_COMMENT` 内联实现

## 附录 C · 工时估算

| 任务 | 工时 | 优先级 | 状态 |
|------|------|--------|------|
| ~~算法补强 · 真太阳时（与通用层联动）~~ | ~~0.5~~ | ✅ 已完成（2026-04-27 · `calculateBazi` 入口接入 `useTrueSolarTime`） | `[x]` |
| ~~算法补强 · 格局识别~~ | ~~0.5~~ | ✅ 已完成（`core/pattern.ts`）| `[x]` |
| ~~算法补强 · 神煞识别（lunisolar plugin-char8ex）~~ | ~~0.5~~ | ✅ 已完成（`core/shensha.ts` + `ShenshaBlock.vue`）| `[x]` |
| ~~数据 · 纳音含义 30 段~~ | ~~0.5~~ | ✅ 已完成（含古籍出处） | `[x]` |
| 数据 · 神煞 long 字段考据补齐 | 1.0 → 实测 0.5 | 🟢 低 | `[x · 60/72 verified]` 2026-04-28 T-2.4-B 推进 17 项（ST3 行号锚点）；剩余 12 项无单立专章按古法标准保留 pending 不编造 |
| ~~数据 · 神煞长释义详情卡 UI（modal）~~ | ~~0.3~~ | ✅ 已完成（`InlineAnnotsBar` 就近展开） | `[x]` |
| 数据 · 大运模板扩展到 100 段 | 1.0 | 🟢 低（M3+） | `[x]` 2026-04-27 |
| 数据 · 流年模板重构 300 段 | 2.0 → 实测 0.5 | 🟡 中（M3 必做） | `[x]` 2026-04-27 |
| ~~UI · 接入 ShareModal（依赖通用层）~~ | ~~0.3~~ | ✅ 已完成 | `[x]` |
| 性能 · 接入 `useDivinationStore` 缓存 | 0.3 | 🟢 低 | `[x · 已决定不接入]` 2026-04-28（实测 1.26ms） |
| 性能 · Web Worker 拆分（如需） | 0.5 | 🟢 低 | `[x · 不需要]` 2026-04-28（实测 1.26ms 远低于 100ms 阈值） |
| **合计（剩余）** | **~4.8 天**（2026-04-27 后：真太阳时 0.5 + 纳音 30 段 0.5 + 神煞 modal 0.3 + 数据 神煞 long 0.5 → 划掉/部分划掉，net ~5.9 → 4.8） | | |

## 附录 D · 风险与对策（设计文档 §10）

| 风险 | 应对 |
|------|------|
| 大运起运算法分歧（流派差异） | 锁定 `tyme4ts` 默认；文档说明流派来源 |
| 子时换日（早子/晚子） | 跟 `tyme4ts` 默认规则；文档明确告知 |
| 真太阳时对小白用户的困惑 | 默认关闭，作为高级选项 |
| 流年模板维度从 30 → 300 段，工程量爆 | ✅ 2026-04-27 规则化生成 300 段（60 甲子干支气势 × 5 旺衰对策），保留 30 段做类型完备性 fallback；后续可针对极端样本人工微调 |
| 移动端大运时间轴显示挤 | 已用 2×5 蛇形网格，避免横向滚动 |
| 流年 10 年挤爆 | 默认仅展示当年，下拉展开 |
| `lunisolar`（神煞）与 `tyme4ts`（四柱）节气定义可能不同 → 同一生辰下神煞所属柱与四柱归属不一致 | `core/shensha.ts` 内部独立用 `lunisolar.fromLunar/fromSolar` 算神煞；与 tyme4ts 的四柱在节气交接附近可能有 1 日偏差，已识别为已知风险（暂未发现实际样本，长期观察）|
| 神煞 plugin 异常会拖垮整张命盘 | `detectShensha` 全局 try/catch + 空数组兜底；UI 走 empty 态 |
| ~~`BaziChart.pattern` 必填但 core 未填 → 隐性类型不安全~~ | ✅ 已解决：`core/pattern.ts → detectPattern` 已在 `calculateBazi` 中填充，类型契约闭合 |
