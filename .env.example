# =============================================================================
# tt-qimen 环境变量样例
#
# 用法：
#   1. 复制本文件为 .env.local（不会被 git 追踪）
#      cp .env.example .env.local
#   2. 按需填入你的真实值；未填的变量会自动 fallback 到 noop
#   3. Vite 仅暴露以 VITE_ 开头的变量给前端代码
# =============================================================================

# -----------------------------------------------------------------------------
# 站点访问量埋点（可选）
# -----------------------------------------------------------------------------
# Google Analytics 4 Measurement ID（格式：G-XXXXXXXXXX）
# - 留空：不加载 gtag 脚本，不发任何请求
# - 填写：注入 gtag 并在 SPA 路由切换时上报 page_view（仅路径，不含 query）
#
# 申请：https://analytics.google.com/ → 媒体资源 → 数据流 → 衡量 ID
# 注意：本站隐私政策已披露使用 GA4，详见 src/locales/{zh-CN,zh-TW,en}.ts privacy 节
VITE_GA4_ID=
