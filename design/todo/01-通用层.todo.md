# 01 · 通用层 · TODO（功能视角）

> 对应设计文档：[`../modules/01-通用层.md`](../modules/01-通用层.md)
> 状态约定：`[x]` 已完成 · `[~]` 部分实现 · `[ ]` 待办
> **本版重写**：2026-04-19 · 改为以"通用层向各模块提供的功能能力"为骨架
> **同步**：2026-04-23 · 巡检 9 个模块真实代码后回写（详情见各模块 todo）：8 个业务模块已 100% 完成 MVP；本通用层"已建未连"项保持原状（divinationStore / FortuneError / ResultCard / 真太阳时 / about 等路由 / 字体栈），但 `useShareCard` composable 已落地并被 6 模块全部接入，附录 C 依赖项 html2canvas / fuse.js / chinese-character-strokes 均已安装

---

## 0. 模块定位

通用层为 8 个占卜模块提供**共享的状态、组件、路由、样式、工具、错误处理、国际化**。本身不直接对用户提供占卜功能，但决定了所有模块的一致性与协同体验。

---

## 1. 功能能力清单（核心）

> 对应设计文档 §1-§7 全部能力点。一行 = 一个对外提供的能力。

| # | 能力 | 设计文档 | 状态 | 实现位置 | 缺口 / 备注 |
|---|------|---------|------|---------|------------|
| 1 | 生辰输入数据结构 `BirthInfo` | §1.1 | `[~]` | `src/stores/user.ts` 中的 `BirthInput` | 缺 `useTrueSolarTime`；多了 `calendar / longitude / birthplace`（差异化扩展） |
| 2 | 用户全局状态 `UserState`（`birth + name`） | §1.2 | `[~]` | `src/stores/user.ts` | 缺 `name: string \| null`（姓名学需要） |
| 3 | 用户态 localStorage 持久化 | §2.1 | `[x]` | `useStorage` 已接入 | — |
| 4 | 主题状态（明暗 + 国风/极简双引擎） | §2.2 | `[x]` | `src/stores/theme.ts` + `composables/useTheme.ts` | — |
| 5 | 多语言状态（zh-CN/zh-TW/en） | 衍生 | `[x]` | `src/stores/locale.ts` + `composables/useLocale.ts` | — |
| 6 | 模块结果缓存 | 衍生 | `[~]` | `src/stores/divination.ts` | 实现完整但**无人调用**（各模块 core 还没接入） |
| 7 | 路由表 + 8 模块懒加载 | §3.1 | `[~]` | `src/router/index.ts` + 8 个模块路由 | 用 hashHistory 不是 webHistory（合理偏离）；缺 `/about` `/disclaimer` `/data-source` `/privacy` 路由 |
| 8 | 路由切换自动 lazy-load 对应模块 i18n 包 | 衍生 | `[x]` | `router.beforeEach` | — |
| 9 | 顶部头 `AppHeader`（导航 + 主题切换 + 语言切换） | §4.1 | `[x]` | `components/layout/AppHeader.vue` | — |
| 10 | 免责声明 `DisclaimerBanner` | §4.2 | `[~]` | 文案内嵌于 AppHeader 顶部横条 | 设计文档原意是独立组件；当前实现等价但未抽出 |
| 11 | 共享生辰输入 `BirthInput.vue` | §4.3 | `[~]` | 实际在 `src/modules/bazi/components/BirthForm.vue` | **反向依赖**：ziwei 模块直接 import bazi 的 BirthForm；缺真太阳时 + 出生地字段；需抽到 `components/common/` |
| 12 | 通用结果卡 `ResultCard`（`level` prop + 三槽位） | §4.4 | `[ ]` | — | 各模块当前内联结果布局；可在第二轮重构时抽 |
| 13 | 分享卡 `ShareModal`（含 html2canvas 导出 PNG + 二维码 + 主题纹样） | §4.5 | `[~]` | `composables/useShareCard.ts` + `components/common/ShareToast.vue` | `html2canvas ^1.4.1` 已装；6 模块（chenggu/lingqian/liuren/xingming/huangli/jiemeng）已接入 useShareCard，导出 PNG + Toast 反馈完成；二维码/主题纹样未做；ShareModal 形态未抽（当前是 page 内 shareCard ref + Toast） |
| 14 | CSS 变量令牌（朱砂红/古铜/鎏金/墨黑/宣纸黄/吉绿/凶红） | §5.1 | `[~]` | `themes/{guofeng,minimal}/tokens.css` | 当前用主题双套实现；与设计文档列出的固定变量名需做命名一致性核对 |
| 15 | 字体栈（思源宋体 / 思源黑体 / JetBrains Mono） | §5.2 | `[ ]` | — | 当前未显式声明字体栈；浏览器 fallback 中 |
| 16 | 响应式断点 mixin（mobile-only / tablet-up / desktop-up） | §5.3 | `[ ]` | 部分 plain CSS @media | 无 SCSS mixin 抽象 |
| 17 | 关键动效（卡片悬停 / 抽签摇晃 / 雷达图增长 / 抽屉滑入） | §5.4 | `[~]` | 雷达图、骨架揭示已做 | 抽签摇晃、抽屉过渡未做（依赖灵签/移动菜单） |
| 18 | 五行查表工具 `utils/element.ts`（`getElementOfGan`） | §6.1 | `[ ]` | — | 八字 core 内部直接用了 tyme4ts，未抽公共 util |
| 19 | 十神算法工具 `utils/tenGod.ts`（`calcTenGod`） | §6.2 | `[ ]` | — | 八字 core 直接用 `dayMaster.getTenStar(stem)`，未抽公共 util |
| 20 | 全局错误结构 `FortuneError`（`{ code, userMessage }`） | §7 | `[ ]` | — | core 异常未统一外抛 |
| 21 | 全局错误边界 + 友好态 | §7 | `[ ]` | — | App.vue 未包 ErrorBoundary；非法日期/跨年子时无统一文案 |

---

## 2. 跨模块的"基础体验流程"完成度

> 用户在任意模块内的完整流程都依赖通用层。

| 步骤 | 期望体验 | 状态 | 备注 |
|------|---------|------|------|
| 1. 首次访问 | URL `?theme=` `?lang=` 引导主题/语言 | `[x]` | `composables/useUrlBootstrap.ts` |
| 2. 选择主题/语言 | 切换即时生效，刷新保持 | `[x]` | `useTheme` `useLocale` + localStorage |
| 3. 进入业务页 | 自动 lazy load 对应模块 + i18n 子包 | `[~]` | 模块 lazy 已生效；i18n 子包**未拆**（详见 §1#22） |
| 4. 输入生辰 | 各模块表单字段一致 | `[~]` | bazi/ziwei 已用同一组件；其他 6 模块尚未涉及 |
| 5. 排盘 | 1.5s 骨架屏 → 渐入 | `[x]` | `useSkeletonReveal` |
| 6. 查看结果 | 主题色编码（吉/中/凶 / 庙旺得利平不陷）| `[~]` | bazi/ziwei 已用；缺通用 ResultCard 抽象 |
| 7. 分享结果 | 一键导出 PNG + 复制链接 | `[~]` | 6 模块（chenggu/lingqian/liuren/xingming/huangli/jiemeng）已接入 useShareCard；ziwei/bazi 仍是静态 UI |
| 8. 跨模块跳转 | 顶栏导航无缝切换 | `[x]` | AppHeader 8 模块导航 |
| 9. 异常发生 | 友好错误态而非白屏 | `[ ]` | 无 ErrorBoundary（见 §1#20-21） |
| 10. 退出/再回 | 状态自动还原 | `[x]` | localStorage |

---

## 3. 子能力详情

### 3.1 国际化（i18n）

| 能力 | 状态 | 实现 |
|------|------|------|
| 三语主文件（zh-CN / zh-TW / en） | `[x]` | `locales/{zh-CN,zh-TW,en}.ts` 各约 290 行 |
| 占卜术语词典 | `[x]` | `locales/divination-terms.{lang}.ts` |
| 模块 i18n 按需懒加载 API | `[x]` | `loadModuleLocale(moduleId, lang)` |
| 模块 i18n 子文件实际拆分 | `[ ]` | `locales/modules/` 仅有 `.gitkeep`，所有模块文案塞在主文件 |
| `<html lang>` 同步 | `[x]` | `loadLocale` 内已写 `documentElement.lang` |
| 术语词典覆盖度（十神 10 + 纳音 30 + 紫微 14 主星 + 6 吉 6 煞 + 4 化）| `[ ]` | 待与各模块 i18n 协同核对 |
| disclaimer / brand / nav / common / footer 五语 key 对齐 | `[x]` | 三语已对齐 |

### 3.2 主题系统

| 能力 | 状态 | 实现 |
|------|------|------|
| 国风主题（朱砂/古铜/鎏金/宣纸） | `[x]` | `themes/guofeng/` |
| 极简主题（中性灰 + 蓝绿点缀） | `[x]` | `themes/minimal/` |
| 共享 base / contracts / tailwind | `[x]` | `themes/_shared/` |
| CSS 按需 lazy import | `[x]` | `themes/index.ts` 中 `themeLoaders` |
| 主题切换时 `data-theme` attribute | `[x]` | `useTheme` |
| shadcn-vue UI 套件 | `[x]` | `components/ui/`（button/label/radio/select 已引入） |

### 3.3 共享 Composables

| Composable | 状态 | 用途 |
|-----------|------|------|
| `useLocale` | `[x]` | 语言切换 + 模块 i18n 包加载 |
| `useTheme` | `[x]` | 主题切换 + CSS 懒加载 |
| `useUrlBootstrap` | `[x]` | URL 引导主题/语言（写 storage） |
| `useUrlSync` | `[x]` | URL 与 store 双向同步（**仅** theme/lang） |
| `useSkeletonReveal` | `[x]` | 排盘骨架 1.5s 揭示 |
| `useSvgDraw` | `[x]` | 八字/紫微关系线绘制 |
| `useShareCard` | `[x]` | html2canvas 导出 PNG + Toast 反馈（6 模块接入） |
| `useBirthStore` | `[x]` | 生辰输入跨模块共享（bazi/ziwei/chenggu 等共用） |
| `useAnnotBar` | `[x]` | 八字四柱标注栏 |

---

## 4. 跨模块约束

| 约束 | 状态 | 说明 |
|------|------|------|
| 所有需要生辰的模块复用同一 BirthInput | `[~]` | bazi 已实现；ziwei 走反向依赖 → 需抽 common；其他 6 模块尚无 |
| 所有 core 函数走 `divinationStore` 缓存 | `[ ]` | store 已建好但无人接入 |
| 所有结果卡走 `ResultCard` 壳 | `[ ]` | 需抽组件 |
| 所有 core 异常外抛 `FortuneError` | `[ ]` | 需统一定义 |

---

## 附录 A · 知识库与数据来源

| 资产 | 类型 | 来源 | 状态 |
|------|------|------|------|
| 共享术语对照表（中/繁/英） | B 类 | 公版整理 | `[~]` 已做 disclaimer/nav/common，业务术语需补齐 |
| About 页内容（数据来源 + AI 辅助声明 + 许可声明） | B 类 | 自撰 | `[ ]` 路由与页面均缺 |

## 附录 B · 数据工程

- [ ] ⏳ 各模块 i18n 拆到 `locales/modules/${id}.{lang}.ts`（避免主文件膨胀）
- [ ] ⏳ 共享术语对照表完善（覆盖所有占卜模块共用术语）
- [ ] ⏳ About 页内容草案

## 附录 C · 依赖与构建

| 依赖 | 用途 | 状态 |
|------|------|------|
| `tyme4ts ^1.4.6` | 八字/黄历/紫微辅助 | `[x]` |
| `iztro ^2.5.8` | 紫微排盘 | `[x]` |
| `@vueuse/core / router ^10.11.0` | composable | `[x]` |
| `pinia / vue-router / vue-i18n` | 状态/路由/i18n | `[x]` |
| `tailwindcss ^4.2.2 / shadcn-vue ^2.6.2 / reka-ui ^2.9.6` | UI | `[x]` |
| `unplugin-auto-import / vue-components` | 自动注册 | `[x]` |
| `html2canvas ^1.4.1` | ShareModal PNG 导出 | `[x]` 已装并被 6 模块经 `useShareCard` 使用 |
| `chinese-character-strokes ^0.0.3` | 姓名学 | `[x]` 已装并按需 dynamic import |
| `fuse.js ^7.3.0` | 解梦搜索 | `[x]` 已装并被 jiemeng 模块使用 |
| `tyme4ts ^1.4.6` | 八字/称骨/紫微辅助/黄历/小六壬 | `[x]` |
| `iztro ^2.5.8` | 紫微排盘 | `[x]` |
| `vite-plugin-compression` | 预压缩 | `[ ]` |
| `rollup-plugin-visualizer` | 体积分析 | `[ ]` |

## 附录 D · 工时估算（剩余）

| 任务 | 工时 | 优先级 |
|------|------|--------|
| BirthInput 抽到 common + 移除反向依赖 + 补真太阳时/出生地 | 0.5 天 | 🔴 高 |
| `UserState.name` 字段 + setter | 0.1 天 | 🔴 高（解锁姓名学） |
| `FortuneError` 类型 + 全局 ErrorBoundary | 0.4 天 | 🟡 中 |
| `utils/element.ts` + `utils/tenGod.ts` 抽离 | 0.3 天 | 🟢 低（八字 core 已可用） |
| `ResultCard` 通用壳 | 0.5 天 | 🟢 低（重构期） |
| `ShareModal` + html2canvas 接入 | 1.0 天 | 🟡 中（合规：分享页带免责） |
| 4 个内容路由（about/disclaimer/data-source/privacy）+ 页面 | 1.0 天 | 🔴 高（合规） |
| 模块 i18n 子文件拆分 | 0.5 天 | 🟢 低 |
| 字体栈与 CSS 变量命名一致性核对 | 0.3 天 | 🟢 低 |
| 关键动效补全（抽签摇晃、抽屉滑入） | 0.3 天 | 跟随业务模块 |
| **合计** | **~5 天** | |

## 附录 E · 风险与对策

| 风险 | 应对 |
|------|------|
| BirthInput 反向依赖蔓延 | 立即抽 common；新模块禁止再加反向依赖 |
| About / Disclaimer 等页缺失影响合规上线 | 上线前必做（验收硬要求） |
| 模块化 i18n 子目录为空，主文件持续膨胀 | 按 `loadModuleLocale` 设计逐模块迁移 |
| 新增依赖（html2canvas / fuse.js / 笔画库）首屏体积膨胀 | 全部走 dynamic import，仅用到时按需加载 |
| `FortuneError` 缺失导致异常裸露 | 优先在 BirthForm 提交边界添加 try/catch |
