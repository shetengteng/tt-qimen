# 11 · 称骨算命模块 · TODO

> 对应设计文档：[`../modules/11-称骨算命模块.md`](../modules/11-称骨算命模块.md)
> 状态约定：`[x]` 已完成 · `[~]` 进行中 · `[ ]` 待办
> **现状盘点已校对**：2026-04-19

---

## 0. 执行摘要

- ❌ **整模块 0% 实现**
- ✅ **基础设施齐**：路由 `/chenggu` 已注册（`router/index.ts`），`tyme4ts ^1.4.6` 已安装含 `SolarDay → LunarDay` 转换能力
- ✅ **复用资产**：`BirthForm`（待从 bazi 抽出到 common）、`useSkeletonReveal`、`CollapsibleSection`、`useDivinationStore`、双主题 CSS、shadcn-vue UI 基础件
- ⚠️ **现状文件**：仅 `src/modules/chenggu/ChengguPage.vue` 168 字节，渲染 `<ModulePlaceholder module-id="chenggu" />`
- 📊 **真实剩余工时**：约 3 天

---

## 1. 类型定义（`src/modules/chenggu/types/index.ts` · **待新建**）

- [ ] `ChengguLevel = 'top' | 'high' | 'middle' | 'low' | 'middleHigh' | 'middleLow'`（设计文档 §4 列了 5 档：上/中上/中/中下/下）
- [ ] `ChengguPoem` — `weight / weightNumber / poem[4] / level / description`
- [ ] `ChengguResult` — `totalWeight / displayWeight / yearWeight / monthWeight / dayWeight / hourWeight / poem`
- [ ] `WeightTableEntry` — 用于 WeightTable 组件的格式化数据

## 2. 核心算法（`src/modules/chenggu/core/chenggu.ts` · **待新建**）

- [ ] `calculateChenggu(birth: BirthInput): ChengguResult` 主入口
- [ ] 调用 `SolarDay.fromYmd(...).getLunarDay()` 取农历对象
- [ ] `getLunarMonth().getLunarYear().getSixtyCycle().getName()` → 年柱干支 → 查 `YEAR_WEIGHT`
- [ ] `lunar.getLunarMonth().getMonth()` → 农历月份 → 查 `MONTH_WEIGHT`
- [ ] `lunar.getDay()` → 农历日 → 查 `DAY_WEIGHT`
- [ ] `Math.floor((birth.hour + 1) / 2) % 12` → 时辰 → 查 `HOUR_WEIGHT`（与 BirthForm hourIdx 规则一致）
- [ ] 总重 = y + m + d + h
- [ ] 在 `POEMS` 中查找（`Math.abs(p.weightNumber - total) < 0.01`）
- [ ] `formatWeight(total: number): string` — 数字 → "四两二"中文（4.2 → "四两二钱"）
- [ ] 错误处理：查不到歌诀 → 抛 `FortuneError({ code: 'CHENGGU_POEM_NOT_FOUND' })`

## 3. 数据（`src/modules/chenggu/data/` · **待新建**）

- [ ] `data/weights.ts`
  - `YEAR_WEIGHT: Record<干支60甲子, number>` — 60 条
  - `MONTH_WEIGHT: Record<月份1-12, number>` — 12 条
  - `DAY_WEIGHT: Record<农历日1-30, number>` — 30 条
  - `HOUR_WEIGHT: Record<时辰0-11, number>` — 12 条
  - **来源**：B 类 · 袁天罡称骨歌公版文献
- [ ] `data/poems.ts` — 51 首歌诀
  - 字段：`weight / weightNumber / poem[4] / level / description`
  - **来源**：B 类 · 公版文献（百度百科通用版）+ C 类 · LLM 白话（人工审校 100%）
- [ ] `data/levelMap.ts` — `weightNumber → ChengguLevel` 5 档分布表

## 4. UI 组件（`src/modules/chenggu/components/` · **待新建**）

- [ ] `WeightTable.vue` — 骨重表 4 列 × 2 行（年/月/日/时 → 分骨重 + 合计）
- [ ] `BalanceAnimation.vue` — 天平动画（CSS keyframes 左右摇晃 3 次后定格）
- [ ] `PoemCard.vue` — 歌诀展示（4 行竖排，宋体大字号，旁边白话解读）
- [ ] `LevelBadge.vue` — 富贵等级徽章（含 5 档配色）
- [ ] **复用** `BirthForm`（依赖通用层 BirthForm 抽出后）
- [ ] **复用** `CollapsibleSection`（`【骨重明细】` + `【歌诀解读】`）

## 5. Composables

- [ ] `composables/useChenggu.ts`
  - 输入：`Ref<BirthInput>`
  - 输出：`{ result: ComputedRef<ChengguResult | null>, loading, error }`
  - 接入 `useDivinationStore` 缓存（key = moduleId + birthHash）

## 6. Page（`src/modules/chenggu/ChengguPage.vue` · **替换占位**）

- [ ] 国风 / 简约 双主题分支（参考 `BaziPage.vue` / `ZiweiPage.vue` 模式）
- [ ] 复用 `useSkeletonReveal`（1.5s 揭示）
- [ ] 标准布局：BirthForm → result-banner → result-zone(WeightTable / PoemCard / LevelBadge) → action-bar(分享/重排盘)

## 7. i18n 文案

- [ ] `locales/modules/chenggu.zh-CN.ts` — 待新建（当前 chenggu 命名空间在主 zh-CN.ts 中仅 nav 项）
- [ ] `locales/modules/chenggu.zh-TW.ts`
- [ ] `locales/modules/chenggu.en.ts`
- [ ] 注册到 `loadModuleLocale('chenggu', lang)`（loader 在 `locales/index.ts` 已就位）
- [ ] 文案：pageTitle / pageSubtitle / breadcrumb / inputCardTitle / btn(paipan/repaipan/share/save) / collapse / weightLabel(year/month/day/hour/total) / level(top/high/middle/middleLow/low) / skeleton

## 8. 路由

- [x] `/chenggu` 路由已注册（`router/index.ts`）
- [x] `ChengguPage.vue` 已 lazy load
- [x] beforeEach 已自动 lazy-load 模块 i18n

## 9. 数据工程

- [ ] **Day 1**：录入 4 个骨重表（共 ~115 条）+ 51 首歌诀原文
  - 多源对照（≥ 2 个来源交叉验证）
  - 用 zod schema 在构建时校验 `weightNumber ∈ [2.2, 7.2]`
- [ ] **Day 1.5**：LLM 生成 51 段白话解读（每段约 80 字，禁"必/绝对"用语）
- [ ] **Day 2**：100% 人工审校 + 措辞统一

## 10. 性能

- [ ] `tyme4ts` 仅 import `SolarDay`（避免引入 `EightChar` 大件）
- [ ] `data/poems.ts` 与 `data/weights.ts` 静态 import（合计 < 30 KB）
- [ ] **目标**：`/chenggu` 路由首屏 gzipped < 110 KB（参考 `20-性能优化.md` §2）

## 11. 测试要点

- [ ] 4 套已知 weightNumber 输入（如 "二两二" / "四两二" / "七两二"）输出正确歌诀
- [ ] 农历跨年子时边界（用 `tyme4ts` 默认规则）
- [ ] 骨重小数精度（2.2 vs 2.20）— 用 `Math.abs(a - b) < 0.01` 比较
- [ ] 51 首歌诀全覆盖（穷举所有合法 weightNumber）

## 12. 验收清单

- [ ] 4 套测试输入全部通过
- [ ] 51 首歌诀 weightNumber 全覆盖（无查不到的合法组合）
- [ ] 白话解读 51 段全部审校
- [ ] 天平动画在主流浏览器流畅播放（60fps）
- [ ] 主题 / 语言切换无破坏

## 13. 知识库映射

| 层 | 类型 | 来源 | 当前状态 |
|---|---|---|---|
| 算法 | 自研（< 50 行） | — | 缺 |
| 4 个骨重表 | B 类 | 袁天罡歌诀公版文献 | 缺 |
| 51 首歌诀 | B 类 | 百度百科通用版 | 缺 |
| 白话解读 | C 类 | LLM × 51 段 + 100% 审校 | 缺 |

## 14. 工时估算

| 任务 | 估算 |
|---|---|
| 4 个骨重表 + 51 首歌诀录入 | 0.5 |
| LLM 生成白话 + 审校 | 0.5 |
| 类型 + `core/chenggu.ts` | 0.3 |
| 4 个 UI 组件（含天平动画） | 1.0 |
| `ChengguPage.vue` + i18n + 联调 | 0.7 |
| **合计** | **3.0 天** |

## 15. 风险与对策

| 风险 | 应对 |
|---|---|
| 农历与公历跨年的边界 | 用 `tyme4ts` 默认规则（与八字一致） |
| 骨重小数精度 | `Math.abs(a - b) < 0.01` 比较 |
| 51 首歌诀版本差异（民间多版本） | 文档注明采用"百度百科通用版"，白话为现代补充 |
| 极少数 weightNumber 查不到 | 显示友好兜底文案 + console.warn |
| BirthForm 当前未抽到 common | 跟随通用层 todo 同步推进 |

