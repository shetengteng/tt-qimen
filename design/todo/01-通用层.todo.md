# 通用层 · TODO

> 对应设计文档：[`../modules/01-通用层.md`](../modules/01-通用层.md)
> 状态约定：`[x]` 已完成 · `[~]` 进行中（含 mock/部分实现） · `[ ]` 待办
> 范围：实现层 + 共享数据/术语层（非模块业务数据）
> **现状盘点已校对**：2026-04-19

---

## 1. 共享类型

- [x] `BirthInput`（命名为 `BirthInput`，定义在 `src/stores/user.ts`）
  - 字段：`calendar / year / month / day / hour / minute / gender / longitude? / birthplace?`
  - 与设计文档 `BirthInfo` 的差异：本实现**多了** `calendar / longitude / birthplace`，**少了** `useTrueSolarTime`
- [ ] 补 `useTrueSolarTime: boolean` 字段（设计文档 §1.1） — 决定是否纳入或用 `longitude` 推算
- [ ] `UserState` 中追加 `name: string | null`（姓名学需要，设计文档 §1.2）
- [ ] `ShareCardData` 类型（分享卡通用结构，供 `ShareModal.vue` 使用）
- [ ] `FortuneError`（统一错误结构 `{ code, userMessage }`） — `core/*` 抛出，UI 层捕获

## 2. Pinia Store

- [x] `stores/user.ts` — `useUserStore`（含 `birth/update/reset`，VueUse useStorage 持久化）
- [ ] `stores/user.ts` — 追加 `name` 字段
- [x] `stores/theme.ts` — `useThemeStore`（GUofeng / Minimal 双主题，CSS 异步懒加载）
- [x] `stores/locale.ts` — `useLocaleStore`（多语切换，i18n 包按需懒加载）
- [x] `stores/divination.ts` — 各模块计算结果按 `moduleId + hash` 缓存
- [ ] 接入：各模块计算函数全部走 `divinationStore.get/set`（目前**无人使用**，因 `core/*` 全缺）

## 3. 路由

- [x] `router/index.ts` — Vue Router 4 + **hashHistory**（设计文档原写 webHistory，实现选 hash 更适合静态部署）
- [x] 8 个模块路由（bazi/ziwei/liuren/chenggu/lingqian/xingming/huangli/jiemeng）+ home + 404
- [x] 路由懒加载（`() => import(...)`）
- [x] 路由 beforeEach 自动 lazy-load 对应模块的 i18n 包
- [x] `scrollBehavior: { top: 0, behavior: 'smooth' }`
- [ ] **`/about` 路由缺失**（设计文档 §3.1 列入）— `AppFooter` 中已有 `#about` 占位链接
- [ ] **`/disclaimer` / `/data-source` / `/privacy` 路由缺失** — `AppFooter` 4 个链接全部为占位 `#xxx`
- [ ] 路由守卫：进入业务页时若无 `user.birth`，自动滚到 `BirthForm`（已有滚动行为，未联动业务）

## 4. 共享组件

### 4.1 已完成

- [x] `components/layout/AppHeader.vue` — 顶栏（双主题，含 8 模块导航 + LangSwitch + ThemeSwitch + 顶部 disclaimer 横条）
- [x] `components/layout/AppFooter.vue` — 页脚（双主题，4 个跳转链接为占位）
- [x] `components/layout/LangSwitch.vue` — 多语切换
- [x] `components/layout/ThemeSwitch.vue` — 主题切换
- [x] `components/common/CollapsibleSection.vue` — 可折叠区块（八字/紫微页大量复用）
- [x] **Disclaimer 文案**：i18n 中 `disclaimer` 字段在 `zh-CN/zh-TW/en` 三套都有，AppHeader 已内嵌渲染（设计文档 §4.2 的 `DisclaimerBanner` 功能由 AppHeader 顶部横条承担）

### 4.2 待办

- [ ] `components/common/BirthInput.vue` — **共享生辰输入**（设计文档 §4.3）
  - 现状：`src/modules/bazi/components/BirthForm.vue` 已实现，**ziwei 模块直接 import 该文件**（反向依赖）
  - 待办：抽出到 `components/common/BirthForm.vue`，移除 ziwei 反向依赖
  - 现有字段：calendar(公/农) + 年 + 月 + 日 + 时辰索引 + 性别
  - **缺**：真太阳时开关、出生地（经度）— 设计文档 §4.3 的可选字段
- [ ] `components/common/ResultCard.vue` — 通用结果卡片壳（slot: header/body/footer + `level` prop 控制吉凶色条）
  - 优先级：低（当前各模块 inline 实现也可，待第二轮统一时再抽）
- [ ] `components/common/ShareModal.vue` — 分享卡（含主题纹样 + 二维码 + 免责声明）
  - 依赖：`html2canvas` 包（package.json 中**未安装**，需 `pnpm add html2canvas`）
  - 各模块底部"分享"按钮当前为静态 UI，未接入

### 4.3 主题/UI 库

- [x] `themes/guofeng/` + `themes/minimal/` 双主题（含 tokens / components / decorations / shadcn 4 个 CSS 文件 × 2 主题）
- [x] `themes/_shared/` 全局基础样式（base / contracts / tailwind）
- [x] `themes/index.ts` — `themeRegistry / themeLoaders`，CSS 按需 import
- [x] `components/ui/`（shadcn-vue 已引入 button / label / radio-group / select）
- [x] `lib/utils.ts`（`cn` 函数）

## 5. Composables（已就位）

- [x] `composables/useLocale.ts`
- [x] `composables/useTheme.ts`（含 CSS 懒加载 + `data-theme` 属性切换）
- [x] `composables/useUrlBootstrap.ts` — 从 URL 引导 theme/lang（写 localStorage）
- [x] `composables/useUrlSync.ts` — URL ?theme= / ?lang= ↔ store 双向同步（**注意：不是同步生辰**）
- [x] `composables/useSkeletonReveal.ts` — 排盘骨架屏 1.5s 后揭示
- [x] `composables/useSvgDraw.ts` — SVG 绘制助手（八字/紫微关系线）

## 6. 样式约定

- [x] CSS 变量分散在 `themes/{guofeng,minimal}/tokens.css`，`themes/_shared/base.css` 提供基础
- [ ] 设计文档 §5.1 列出的具体变量名（`--color-primary` / `--color-ink` 等）核对一致性
- [ ] 字体栈核对：设计文档 §5.2 的 `Source Han Serif SC` / `JetBrains Mono` 是否生效
- [ ] 响应式断点 mixin（`mobile-only` / `tablet-up` / `desktop-up`） — 当前为 plain CSS，无 mixin

## 7. 共享 Utils（待办）

- [ ] `src/utils/element.ts` — 五行查表（`ELEMENTS` 常量 + `getElementOfGan(gan)`）
  - 现状：八字 `data/tenGods.ts` 仅含十神含义，**五行查表函数缺失**
- [ ] `src/utils/tenGod.ts` — 十神算法 `calcTenGod(dayGan, otherGan)`
  - 现状：`tenGods.ts` 仅有**含义文案**（10 段完整），**算法函数缺失**
- [ ] `src/utils/dateFormat.ts` — 公历/农历日期格式化（zh/zh-TW/en 三种）
- [ ] `src/utils/share.ts` — 生成分享 URL / 复制剪贴板封装

## 8. 全局错误处理

- [x] `src/modules/_404.vue` — 404 页
- [ ] 全局错误边界（App.vue 包一层 `<ErrorBoundary>` 或 Vue 3 `errorCaptured`）
- [ ] `core/*` 函数统一抛 `FortuneError`，UI 层 try/catch 后显示友好态
- [ ] 非法日期 / 跨年子时边界的友好提示文案（i18n）

## 9. 国际化（i18n）

- [x] `locales/index.ts` — i18n 实例 + lazy-load `loadLocale(lang)` + lazy-load `loadModuleLocale(moduleId, lang)`
- [x] `locales/zh-CN.ts` / `zh-TW.ts` / `en.ts` — 三语主文件（约 290 行/语言，覆盖通用 + 8 模块基础）
- [x] `locales/divination-terms.{zh-CN,zh-TW,en}.ts` — 占卜术语词典
- [x] **disclaimer / brand / nav / common.placeholder / footer 四语完整对齐**
- [ ] **`locales/modules/` 目录仅有 `.gitkeep`** — 模块化 i18n 文件**全部缺失**（loadModuleLocale 设计为按需加载 `modules/${moduleId}.${lang}.ts`，目前所有模块文案都塞在主文件 zh-CN.ts 中）
- [ ] 切换 locale 时 `<html lang>` 同步（`useTheme` 已切 `data-theme`，`loadLocale` 已切 `documentElement.lang` ✅ **核查后已实现**）
- [ ] 术语词典是否覆盖八字/紫微所有专有名词（十神 10 + 纳音 30 + 紫微 14 主星 + 6 吉 6 煞 + 4 化）— 待核对

## 10. 数据工程（共享层）

- [ ] B 类·共享术语对照表（中 / 繁 / 英），覆盖所有占卜模块共用术语
  - 工时：0.5 天
- [ ] About 页内容草案（含数据来源声明 + AI 辅助生成声明 + MIT/Apache 许可声明）

## 11. 依赖与构建

- [x] `tyme4ts ^1.4.6` 已安装
- [x] `@vueuse/core ^10.11.0` + `@vueuse/router ^10.11.0` 已安装
- [x] `pinia ^2.2.2 / vue-router ^4.4.3 / vue-i18n ^9.14.0` 已安装
- [x] `tailwindcss ^4.2.2 / shadcn-vue ^2.6.2 / reka-ui ^2.9.6` 已安装
- [x] **`iztro ^2.5.8` 已安装并接入**（`src/modules/ziwei/core/ziwei.ts`）
- [x] `unplugin-auto-import ^0.18.3 / unplugin-vue-components ^0.27.4` 已安装（自动注册）
- [ ] `html2canvas` — 分享卡导出，**待安装**
- [ ] `chinese-character-strokes` — 姓名学，**待安装**
- [ ] `fuse.js` — 解梦搜索，**待安装**
- [ ] `vite-plugin-compression` — 预压缩 `.gz` / `.br`，**待安装**
- [ ] `rollup-plugin-visualizer` — 体积分析，**待安装**

## 12. 验收清单（通用层）

- [x] 8 个模块路由全部可达，无 404
- [ ] 切换主题（guofeng ↔ minimal）所有页面无样式破坏（待逐页验证）
- [ ] 切换语言（zh-CN ↔ zh-TW ↔ en）所有页面文案完整无 fallback（占位模块 6 个尚无业务文案）
- [ ] BirthInput 在所有需要的模块中行为一致（目前仅 bazi + ziwei 接入）
- [x] 免责声明在 AppHeader 顶部横条始终可见
- [x] localStorage 中 `tt-qimen:birth` / `:theme` / `:locale` 跨页面、跨刷新保持

## 13. 风险与对策

| 风险 | 应对 |
|---|---|
| `BirthForm` 未抽到 common，紫微页反向依赖 bazi 模块 | M2 完成八字真实排盘后立即抽出；新模块禁止再加反向依赖 |
| Share / About 等页面缺失影响合规 | 上线前必做（验收硬要求）|
| 模块化 i18n 子目录为空，文案膨胀于主文件 | 按 `loadModuleLocale` 设计：业务模块文案迁移到 `locales/modules/${id}.${lang}.ts` |
| 新增依赖（html2canvas、fuse.js 等）首屏体积膨胀 | 全部走 dynamic import，仅在用到的模块按需加载 |

