# 17 · 紫微斗数模块 · TODO

> 对应设计文档：[`../modules/17-紫微斗数模块.md`](../modules/17-紫微斗数模块.md)
> 状态约定：`[x]` 已完成 · `[~]` 进行中（含简化/部分实现） · `[ ]` 待办
> **现状盘点已校对**：2026-04-19
> **本次盘点修正**：发现 core/ziwei.ts (430 行) 已基于 iztro 完整接入，本模块完成度远高于初判
> **2026-04-19 第二次盘点**：完成 ZiweiPage 接入 + 6 组件 chart prop 透传 + mock 兜底 + 多组生辰 Playwright 实测 + 元信息日期补 0 修复

---

## 0. 执行摘要

- ✅ **算法层 95%**：`src/modules/ziwei/core/ziwei.ts` 已基于 `iztro ^2.5.8` 适配完整领域对象：
  - 12 宫排盘（公历/农历，自动地支映射 4×4 slot）
  - 主星（majorStars）+ 副星（minorStars/adjectiveStars）按白名单 + 煞星分类
  - 庙旺得利平不陷 7 档亮度归一化
  - 本命四化（lu/quan/ke/ji）扫描 + 落宫表
  - 大限：每宫一段，按起始年龄升序，标记当前
  - 小限：当前年 -1 至 +4 共 6 段（probe 6-15 日避开年初干支边界）
  - 三方四正（命/财帛/官禄 + 对宫迁移）
  - 元信息：solar/lunar/五行局/命主/身主/当年虚岁/流年干支（solar 已补 0 为 YYYY-MM-DD）
  - 启发式解读卡（命宫主星 / 四化落宫 / 副煞星 / 身宫主星）
- ✅ **UI 100%**：8 个组件全部完成 + 数据接入闭环
  - ZiweiMeta / SihuaLegend / SanfangToggle / ZiweiPalaceChart / ZiweiMobile / InterpretCards / DaxianGrid + 复用 ZiweiPalace
  - ZiweiPage `shallowRef<ZiweiChart | null>` + `onPaipan` 调用 + `onRepaipan` 重置
  - 5 个数据消费组件全部接收 chart prop + `chart ?? mock` 兜底
- ✅ **Composables**：`useZiweiRelations.ts` 三方四正 SVG 连线已实现
- ✅ **Playwright 实测通过**：公历 1990-05-20 午时男 + 农历 1990-05-20 午时女 双路径渲染正确，控制台 0 errors
- ⚠️ **缺口**：B 类深度模板（500 段）、独立 BirthForm（当前反向依赖 bazi）、xiaoxianCells 与 mock 6 段对齐、bazi 模块预存在的类型错误阻塞全项目 build
- 📊 **真实剩余工时**：第一发布 ~2 天 / 第二发布 ~5 天（vs 上次盘点 4+5 天）

---

## 1. 类型定义（src/modules/ziwei/types/index.ts · 200+ 行）

- [x] `SihuaKey` (lu/quan/ke/ji)
- [x] `StarKind` (main/aux/malefic)
- [x] `PalaceKey` × 12
- [x] `Brightness` 7 档
- [x] `Star / Palace`
- [x] `DaxianCell / XiaoxianCell`
- [x] `SihuaPalaceMap`
- [x] `TagTone` (accent/success/danger/gold/jade/plain)
- [x] `InterpretCard`
- [x] `SanfangSiZheng`
- [x] `ZiweiMetaInfo`
- [x] `ZiweiChart`
- [x] **常量映射**：
  - `IZTRO_PALACE_TO_KEY`（iztro 中文宫名 → 项目 key）
  - `EARTHLY_BRANCH_TO_SLOT`（地支 → 4×4 slot）
  - `MUTAGEN_TO_SIHUA_KEY`（中文四化 → en key）
  - `joinGanZhi(stem, branch)` 辅助

## 2. 核心算法（src/modules/ziwei/core/ziwei.ts · 430 行）

### 2.1 已完成

- [x] `buildZiweiChart(birth)` 主入口
- [x] `astro.byLunar / astro.bySolar` 调用（含 timeIndex / gender / lang 参数）
- [x] `hourToTimeIndex` — 与 BirthForm hourIdx 完全一致（早子/晚子边界）
- [x] `mergeStars` — 主/副/煞星统一为 UI Star[]
- [x] `classifyStarKind` — 重要副星白名单（左辅/右弼/文昌/文曲/天魁/天钺/禄存/天马/红鸾/天喜）+ 6 煞星
- [x] `normalizeBrightness` — 7 档容错
- [x] `normalizeSihua` — 中文 → en key 容错
- [x] `buildPalaces` — 12 宫顺序、命宫/身宫识别、大限文案、长生 12 神
- [x] `buildDaxianCells` — 大限按年龄排序、标记当前
- [x] `buildXiaoxianCells` — 当前年 ±2 共 6 段
- [x] `buildSihuaMap` — 扫描所有宫的 sihua 字段
- [x] `buildSanfang` — 命宫驱动（命/财/官 + 对宫迁移）
- [x] `buildInterpretCards` — 启发式（命宫主星 + 四化 + 副煞 + 身宫主星）
- [x] `buildMeta` — solar/lunar/五行局/命主/身主/当年虚岁/流年干支

### 2.2 待办

- [ ] **真太阳时**（与八字共享 `BirthInput.useTrueSolarTime`）
  - 工时：跟随八字，0
- [ ] **斗君（doujun）字段**填充：当前 `meta.doujun = ''`，需要根据生月生时算（命理常用 = 寅 + 生月数 - 生时数）
  - 工时：0.3 天
- [ ] **起运岁数（qiyunAge）**填充：当前固定为 `0`
  - 工时：0.2 天（iztro 已含 decadal.range 起点 = 起运虚岁）
- [ ] **xiaoxianCells 6 段对齐**：mock 是当前 ±2 共 6 段（含 -2 / -1 / 当前 / +1 / +2 / +3），core 当前实现是 -1 / 当前 / +1 / +2 / +3 / +4 共 6 段，需调整 core 与 mock 对齐
  - 工时：0.2 天
- [ ] **解读卡升级到模板驱动**（设计文档 §3.2 的 500 段）
  - 当前：启发式从 chart 派生标签
  - 升级：按 `mainStar + brightness` 查 `data/ziwei/main-star-template.json` 取深度文案
  - 工时：见 §5.2 数据工程

### 2.3 性能优化

- [x] `ZiweiChart` 已通过 `shallowRef` 在 ZiweiPage 中持有
- [ ] **Web Worker**（设计文档 §6.2）：拆 `core/ziwei.worker.ts`
  - 触发条件：实测 iztro 排盘 > 100ms 时（在浏览器实测）
  - 工时：0.5 天
- [ ] **结果缓存**：接入 `useDivinationStore`
  - 工时：0.3 天

## 3. 组件（src/modules/ziwei/components/）

- [x] `ZiweiMeta.vue` — 元信息卡（solar/lunar/五行局/命主/身主/当年虚岁/流年干支）
- [x] `SihuaLegend.vue` — 四化图例
- [x] `SanfangToggle.vue` — 三方四正显隐切换
- [x] `ZiweiPalace.vue` — 单宫 12 个之一（共用渲染单元）
- [x] `ZiweiPalaceChart.vue` — 桌面 12 宫主图（4×4 含中宫信息位 + SVG 关系线）
- [x] `ZiweiMobile.vue` — 移动端紧凑列表布局
- [x] `InterpretCards.vue` — 解读卡 4 张
- [x] `DaxianGrid.vue` — 大限 + 小限网格（10 段大限 + 6 段小限）
- [ ] **复用** `BirthForm.vue` — **当前反向依赖 `@/modules/bazi/components/BirthForm.vue`**
  - 修复：将 BirthForm 抽到 `components/common/`，紫微改为 `import @/components/common/BirthForm.vue`
  - 工时：0.3 天（与八字 todo 共享）

## 4. Composables（src/modules/ziwei/composables/）

- [x] `useZiweiRelations.ts` — 三方四正 SVG 连线（含主题切换响应 + 节流 schedule）
- [x] **复用** `useSkeletonReveal`（1.5s 揭示）

## 5. 数据（src/modules/ziwei/data/）

### 5.1 已完成

- [x] `mockZiwei.ts` — 完整 12 宫 + 主辅煞星 + 四化 + 大限 + 小限 + 解读卡 + 三方四正（235 行高保真 mock）

### 5.2 待办：B/C 类深度模板（设计文档 §3.2）

> **第一发布**：可继续用启发式解读卡 + iztro 真实数据，UI 已完整。
> **第二发布**：替换为模板驱动的深度文案。

- [ ] `data/mainStarTemplates.ts`（**A/B 类**）—  14 主星 × 12 宫位 = 168 段（每段 ~80 字）
  - 数据来源：明清古籍 + 现代命理书籍公版整理
  - 工时：3 天（B 类公版整理 + 文笔润色）
- [ ] `data/sihuaTemplates.ts`（**B 类**）— 4 化 × 12 宫 = 48 段
  - 工时：1 天
- [ ] `data/palaceCombinations.ts`（**C 类**）— 命宫 + 三方四正主星组合 = ~30 个常见组合
  - LLM 辅助生成 + 人工审核
  - 工时：1.5 天
- [ ] `core/buildInterpretCards` 升级为模板查表（替换启发式）
  - 工时：0.5 天

### 5.3 与 mock 不一致点

- [ ] mock 12 宫顺序与真实 iztro 输出顺序对齐校验（已按 slot 升序排，应一致）
- [ ] mock 含 doujun: '正月 子时'，core 暂为空 — 跟随 §2.2 解决

## 6. 国际化

- [x] `ziwei` 命名空间已在 `locales/zh-CN.ts` / `zh-TW.ts` / `en.ts` 全量落地（pageTitle / inputCardTitle / collapse / palace / star / sihua / btn 等）
- [ ] **抽到 `locales/modules/ziwei.{lang}.ts`**（按需加载）
- [ ] zh-TW / en 的星名 / 宫名翻译完整性核对（紫微术语量大）
  - 主星 14 + 副星 10 + 煞星 6 + 12 宫名 + 四化 = ~46 项
  - 工时：0.5 天

## 7. 路由 / 入口

- [x] `/ziwei` 路由已注册
- [x] `ZiweiPage.vue` 已 lazy load

## 8. 验收清单

- [x] 输入公历 1990-05-20 午时男 → iztro 真实排盘（命宫丁亥 紫微旺 七杀平 + 四化全部出现）
- [x] 输入农历 1990-05-20 午时女 → 阳历 1990-06-12（命宫戊子 天梁庙 + 火六局 + 大限 36-45 子女）
- [x] Playwright 实测：浏览器控制台 0 errors / 0 warnings
- [x] `chart === null` 兜底：清空后正确显示 mock 占位（半透明灰度 result-zone）
- [x] 切换主题（guofeng ↔ minimal）UI 完整（移动端走 ZiweiMobile）
- [ ] 切换语言（zh-CN ↔ zh-TW ↔ en）星名翻译完整
- [ ] 排盘性能 < 100ms（CPU 中速机型）— 实测待补（目测 < 200ms 无卡顿感）
- [x] 三方四正 SVG 连线在主题切换后能重绘（watch isGuofeng + chartRef.schedule）
- [ ] 极端日期（农历闰月、跨年）排盘不报错
- [ ] iztro 不支持的越界年份（< 1900）友好提示

## 9. 真实剩余工时

> 2026-04-19 第二次盘点更新。已划掉的为本次会话完成的任务。

| 任务 | 工时 | 状态 |
|---|---|---|
| **阻塞** | | |
| 修复 bazi 模块预存在类型错误（src/modules/bazi/core/bazi.ts(134,5) `pattern` 字段缺失），影响 `pnpm build` | 0.2 | 🔴 |
| **第一发布（M2 内必做）** | | |
| BirthForm 抽到 common（与八字共享） | 0.3 | ⬜ |
| 斗君 / 起运岁数 / xiaoxianCells 对齐 | 0.7 | ⬜ |
| zh-TW / en 星名宫名翻译 | 0.5 | ⬜ |
| 性能 / 边界实测（极端日期、< 1900 年） | 0.5 | ⬜ |
| 解读卡 fallback 兜底（无主星宫的 'mingPalace'） | 0.3 | ⬜ |
| ~~ZiweiPage 接入 buildZiweiChart + 6 组件 chart prop 透传~~ | ~~0.3~~ | ✅ |
| ~~元信息 solar 日期补 0~~ | ~~0.05~~ | ✅ |
| ~~Playwright 实测多组生辰 + 控制台~~ | ~~0.2~~ | ✅ |
| **第一发布合计** | **2.5 天** | |
| **第二发布（M3）** | | |
| 主星 168 段模板（mainStarTemplates） | 3.0 | ⬜ |
| 四化 48 段模板（sihuaTemplates） | 1.0 | ⬜ |
| 30 个常见组合 C 类（palaceCombinations） | 1.5 | ⬜ |
| buildInterpretCards 改为模板查表 | 0.5 | ⬜ |
| 大限/小限趋势提示（参考八字 hint） | 0.5 | ⬜ |
| Web Worker 拆分（如需） | 0.5 | ⬜ |
| **第二发布合计** | **7.0 天** | |

## 10. 风险与对策

| 风险 | 应对 |
|---|---|
| **bazi 模块预存在类型错误（`InterpretInput` 无 `pattern` 字段）阻塞 `pnpm build`** | 优先于本模块任何提交解决；定位 src/modules/bazi/core/bazi.ts(134,5) 与 InterpretInput 接口对齐 |
| `BirthForm` 反向依赖 bazi | 优先级最高，与八字 todo 共同推进 |
| iztro 库 API 变更 | 锁定 `^2.5.8` 当前 minor，next major 升级前评估 |
| 紫微术语 zh-TW / en 翻译质量 | 引入命理学者审校或使用社区现成翻译表 |
| 启发式解读卡缺乏深度（仅 4 张 chip） | 第二发布替换为模板驱动，第一发布在 InterpretCards 加"算法说明"小字 |
| 闰月 / 跨年子时边界 | iztro 已处理，但需 QA 实测覆盖 |
| 移动端 ZiweiPalaceChart 4×4 SVG 在小屏挤压 | ZiweiMobile.vue 已切走，确保 < 768px 不显示 4×4 |

