# 10 · 八字模块 · TODO

> 对应设计文档：[`../modules/10-八字模块.md`](../modules/10-八字模块.md)
> 状态约定：`[x]` 已完成 · `[~]` 进行中（含简化/部分实现） · `[ ]` 待办
> **现状盘点已校对**：2026-04-19
> **本次盘点修正**：发现 core/bazi.ts (568 行) 与 data/* 全部已落地，本模块完成度远高于初判

---

## 0. 执行摘要

- ✅ **算法层 100%**：`src/modules/bazi/core/bazi.ts` 已基于 `tyme4ts` 完整实现：
  - 四柱排盘（公历/农历，自动节气换月）
  - 十神（用 `dayMaster.getTenStar(stem)` 直读 tyme4ts）
  - 藏干 + 五行权重（本气 0.7 / 中气 0.2 / 余气 0.1，月令 ×1.5）
  - 旺衰 5 档（极旺/偏旺/中和/偏弱/极弱）
  - 喜用神/忌神（弱→印比 / 旺→官杀食伤 / 中和→平衡）
  - 大运（`ChildLimit.getStartDecadeFortune()` 取 8 段，含起运年月）
  - 流年（当年 + 后 9 年）
  - 命盘简析（模板拼装两段 ~150 字 + 4 chip）
- ✅ **UI 100%**：8 个组件全部完成（FourPillarsTable / ShishenStructure / ElementsRadar / InterpretBlock / DayunTimeline / FlowYears / BirthForm / 复用 CollapsibleSection）
- ✅ **数据 100%**：tenGods (10 段) / fortuneHints (30 段) / flowYearHints (30 段) / interpretTemplate（旺衰描述 + 五行 tips）全部落地
- ⚠️ **缺口**：格局识别（types 定义了 `PatternInfo` 但 `core` 未填充，UI 当前使用占位"常规"）+ 真太阳时 + 神煞表 + Web Worker
- 📊 **真实剩余工时**：~3 天（vs 初判的 9.3 天）

---

## 1. 类型定义（src/modules/bazi/types/index.ts）

- [x] `ElementName / TenGodType / StrengthLevel / Tendency`（基础枚举）
- [x] `PillarInfo`（含 gan/zhi/ganzhi/五行/阴阳/纳音/十神/藏干）
- [x] `HideStem`（含 weight 0.7|0.2|0.1 + type main/middle/residual）
- [x] `ElementsScore` + `ElementCell`（含 status: strong/balanced/weak）
- [x] `DecadeFortune`（含 startAge/endAge/startYear/endYear/tenGod/tendency/hint/current）
- [x] `FlowYear`（含 year/ganzhi/十神/tendency/hint/tags/current）
- [x] `InterpretSummary`（paragraph1/paragraph2/tags）
- [x] `ChartMeta`（solar/solarEn/lunar/lunarEn/genderTitle）
- [x] `BaziChart`（顶层聚合）
- [x] `PatternName / PatternInfo`（格局类型）
- [ ] **类型不一致修复**：`BaziChart.pattern: PatternInfo` 是必填，但 `core/bazi.ts` 未在返回值中包含 `pattern` 字段 → 编译能过是因为 ts 宽容；需要：
  - 选项 A：实现 `pickPattern()` 函数填充
  - 选项 B：将 `pattern` 改为可选 `pattern?: PatternInfo`
- [ ] `BirthInput.useTrueSolarTime?: boolean`（在 `stores/user.ts`）— 真太阳时支持

## 2. 核心算法（src/modules/bazi/core/bazi.ts · 568 行）

### 2.1 已完成

- [x] `calculateBazi(birth)` 主入口（公历/农历输入归一化 → SolarTime → EightChar）
- [x] `toSolarTime`：公历直走 `SolarTime.fromYmdHms`；农历走 `LunarHour → getSolarTime()`
- [x] `buildPillar`：单柱组装（含十神、藏干、纳音、阴阳）
- [x] `tenStarFromStem`：基于 tyme4ts `dayMaster.getTenStar(stem).getIndex()`
- [x] `collectHideStems`：依 tyme4ts `HideHeavenStemType.{MAIN,MIDDLE,RESIDUAL}` 分类
- [x] `sumElements`：天干 1.0 + 藏干权重，月令五行 ×1.5
- [x] `buildElementCells`：占比 + 状态阈值（>120% strong / <80% weak）
- [x] `judgeStrength`：5 档（基于 me/avg 比值 1.6/1.2/0.8/0.4 阈值）
- [x] `pickFavorable`：MVP 简化版（弱→印比 / 旺→官杀食伤 / 中和→平衡）
- [x] `buildDecades`：起运 + 顺/逆排 8 段（用 ChildLimit）
- [x] `buildFlowYears`：当年 + 后 9 年（10 年）
- [x] `judgeTendency`：吉/中/凶（命中喜用 → favorable / 命中忌神 → unfavorable）
- [x] `buildMeta`：公历/农历字符串 + 乾造/坤造
- [x] `buildInterpret`：模板字符串拼装两段简析

### 2.2 待办

- [ ] **格局识别**（设计文档 §2.4）：实现 `pickPattern(eightChar, tenGods)` → `PatternInfo`
  - MVP：8 大正格 + 建禄/月刃 / 杂气 = 11 格
  - 算法：以月支本气十神为基础 → 看年/月/时柱透干 → 命名格局
  - 当前：`interpretTemplate.ts.PATTERN_DEFAULT` 写死为"常规 · 相对均衡"
  - 工时：0.5 天
- [ ] **真太阳时**：`birth.useTrueSolarTime === true` 时，按 `birth.longitude` 算时差矫正再喂给 SolarTime
  - 工时：0.5 天
- [ ] **格局识别后的 InterpretSummary 联动**：填入 `paragraph2` 的 `{pattern}` 与 `{relation}`，移除 `PATTERN_DEFAULT` 兜底
- [ ] **关系自动扫描**（替换 BaziPage 中固定的"冲/自刑/暗合"示意）：
  - 三合（申子辰/亥卯未/寅午戌/巳酉丑）
  - 六合（子丑/寅亥/卯戌/辰酉/巳申/午未）
  - 六冲（子午/丑未/寅申/卯酉/辰戌/巳亥）
  - 三刑（寅巳申/丑戌未/子卯/辰辰、午午、酉酉、亥亥自刑）
  - 工时：1 天
- [ ] **神煞表**（可选，设计文档 §2.5）：天乙贵人、文昌、桃花、华盖、驿马等 8-12 个常用神煞
  - 工时：1 天

### 2.3 性能优化

- [x] `BaziChart` 已通过 `shallowRef` 在 BaziPage 中持有，避免深响应
- [ ] **Web Worker**（设计文档 §6.2）：将 `calculateBazi` 拆到 `core/bazi.worker.ts`，主线程仅 postMessage
  - 触发条件：实测排盘耗时 > 100ms 时
  - 工时：0.5 天
- [ ] **结果缓存**：接入 `useDivinationStore`，相同 BirthInput hash 直接返回
  - 工时：0.3 天

## 3. 组件（src/modules/bazi/components/）

- [x] `BirthForm.vue` — 生辰录入（公/农 + 年月日时辰 + 性别 + Select 联动校验日期上限）
- [x] `FourPillarsTable.vue` — 四柱表 + 关系示意 SVG（年/月/日/时）
- [x] `ShishenStructure.vue` — 十神结构图（年/月/时三干十神）
- [x] `ElementsRadar.vue` — 五行雷达图（5 元素，含 strong/balanced/weak 着色）
- [x] `InterpretBlock.vue` — 命盘简析两段
- [x] `DayunTimeline.vue` — 大运 8 段时间轴（含连接线 SVG）
- [x] `FlowYears.vue` — 流年 10 年（含 tag chip）
- [x] **复用** `CollapsibleSection`（命盘/简析/大运/流年 4 折叠区）
- [ ] **`BirthForm` 抽到 `components/common/`**（紫微反向依赖问题）
- [ ] **`BirthForm` 增字段**：真太阳时开关 + 出生地（经度）
- [ ] **格局展示**：`ShishenStructure` 或新建 `PatternBadge.vue` 展示格局名 + 一句话点评

## 4. Composables（src/modules/bazi/composables/）

- [x] `useBaziDrawings.ts` — 关系线 SVG 调度（含主题切换 + 节流）
  - 关系示意 SVG（FourPillarsTable）+ 大运连接线 SVG（DayunTimeline）
- [x] **复用** `useSkeletonReveal`（1.5s 后揭示结果区，含滚动）
- [x] **复用** `useSvgDraw`（被 useBaziDrawings 内部使用）
- [ ] `useBirthValidation.ts` — 抽出生辰输入的边界校验（无效日期、跨年子时提示）
  - 现状：BirthForm 内 watch 收敛 day 上限，但无显式错误态
  - 工时：0.3 天

## 5. 数据（src/modules/bazi/data/）

### 5.1 已完成

- [x] `tenGods.ts` — 十神定义 + 80 字 desc + 30 字 descMn（10 段，B 类公版整理）
- [x] `fortuneHints.ts` — 大运一句话模板（10 十神 × 3 吉凶 = **30 段**）
- [x] `flowYearHints.ts` — 流年一句话 + tag chip（**30 段**）
- [x] `interpretTemplate.ts` — 命盘简析模板（PARAGRAPH_1/2 + STRENGTH_DESC + ELEMENT_TIPS）
- [x] `mockBazi.ts` — UI 兜底 mock（首次进入 / 计算失败时使用）

### 5.2 与设计文档差异

> 设计文档 §3.4 说"100 段"，实际为"30 段单变体"。
> 这等同于 30 段 × 3 措辞变体未做（用户不会看出差异，**MVP 充分**）。

- [ ] **可选**：扩展模板措辞变体（每段提供 3 个改写版本，按 hash 选取，避免重复感）
  - 工时：1 天（B 类公版整理 + 文笔润色）
  - 优先级：低（M3+ 再做）

### 5.3 神煞表（可选 · 设计文档 §3.5）

- [ ] `data/shenshaTable.ts` — 8-12 个常用神煞的查表（天乙贵人、文昌、桃花等）
  - 数据规模：~12 段，每段 30 字描述
  - 工时：0.5 天

### 5.4 格局表

- [ ] `data/patternTemplates.ts` — 11 个格局名 + 一句话点评（替换 PATTERN_DEFAULT）
  - 工时：0.3 天

## 6. 国际化

- [x] `bazi` 命名空间已在 `locales/zh-CN.ts` / `zh-TW.ts` / `en.ts` 全量落地
  - hours / calendar / field / gender / inputCardTitle / chartMeta / collapse / fortune / flow / btn / pageTitle / pageSubtitle / resultBanner / relations
- [ ] **抽到 `locales/modules/bazi.{lang}.ts`**（i18n 已设计为模块按需加载，但目前仍合并在主 bundle）
  - 工时：0.3 天

## 7. 路由 / 入口

- [x] `/bazi` 路由已注册
- [x] `BaziPage.vue` 已在路由 lazy load

## 8. 验收清单

- [x] 输入公历 1990-05-20 午时男 → 真实排盘（庚午年/辛巳月/丁亥日/丙午时）
- [x] 输入农历 1990-04-26 午时男 → 走 LunarHour 路径，结果与公历一致
- [x] 切换主题（guofeng ↔ minimal）UI 完整
- [ ] 切换语言（zh-CN ↔ zh-TW ↔ en）大运/流年提示语翻译完整（zh-CN ✅，zh-TW/en 仅 UI 文案，**模板文案未译**）
- [ ] 排盘性能 < 100ms（CPU 中速机型）— 实测待补
- [ ] 跨年子时（23:00-00:59）边界正确（设计文档 §7.2）— 实测待补
- [ ] 1900 年前出生 → 友好提示（设计文档 §7.3）— 当前 BirthForm yearOptions 限制到 1900-current

## 9. 真实剩余工时

| 任务 | 工时 |
|---|---|
| BirthForm 抽到 common + 真太阳时字段 | 0.5 |
| 真太阳时算法（longitude 矫正） | 0.5 |
| 格局识别 (`pickPattern` + 11 段模板) | 0.5 |
| 关系自动扫描（合冲刑害） | 1.0 |
| zh-TW / en 模板文案翻译 | 0.5 |
| **MVP 必做合计** | **3.0 天** |
| 神煞表（8-12 段） | 0.5 |
| Web Worker 拆分 | 0.5 |
| 模板 3 措辞变体 | 1.0 |
| **可选合计** | **2.0 天** |

## 10. 风险与对策

| 风险 | 应对 |
|---|---|
| 喜用神 MVP 简化版与传统命理学差异较大 | 在 InterpretBlock 底部加"算法说明"小字 + 链接到 About 页 |
| `BaziChart.pattern` 类型必填但 core 未实现 | 立即修复（选项 A：实现；选项 B：改可选） |
| 流年模板 30 段在 10 年内可能重复 | 先观察 M2 用户反馈，再决定是否扩 90 段 |
| BirthForm 在 ziwei 反向依赖 | 抽到 common 时同步迁移 |

