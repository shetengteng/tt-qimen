# 12 · 小六壬模块 · TODO

> 对应设计文档：[`../modules/12-小六壬模块.md`](../modules/12-小六壬模块.md)
> 状态约定：`[x]` 已完成 · `[~]` 进行中 · `[ ]` 待办
> **现状盘点已校对**：2026-04-19

---

## 0. 执行摘要

- ❌ **整模块 0% 实现**
- ✅ **基础设施齐**：路由 `/liuren` 已注册（`router/index.ts`），`tyme4ts ^1.4.6` 可用于 `SolarDay → LunarDay` 农历转换
- ✅ **算法极简**：核心仅 5 行（6 宫循环 mod 6 推演 3 步）
- ✅ **复用资产**：`useDivinationStore`、`useSkeletonReveal`、`CollapsibleSection`、双主题 CSS、shadcn-vue 基础件
- ⚠️ **现状文件**：仅 `src/modules/liuren/LiurenPage.vue` 167 字节（占位）
- 🎯 **体验定位**："今日运势"快测入口，1 分钟出结果（区别于八字/紫微的深度排盘）
- 📊 **真实剩余工时**：约 2 天

---

## 1. 类型定义（`src/modules/liuren/types/index.ts` · **待新建**）

- [ ] `PalaceName = '大安' | '留连' | '速喜' | '赤口' | '小吉' | '空亡'`
- [ ] `JiXiong = 'ji' | 'xiong' | 'neutral'`
- [ ] `Aspect = 'overall' | 'career' | 'love' | 'wealth' | 'health'`
- [ ] `ElementCN = '木' | '火' | '土' | '金' | '水'`
- [ ] `PalaceReading` — `name / ji_xiong / element / symbol / readings(Record<Aspect, string>) / suitable[] / avoid[]`
- [ ] `LiurenInput` — `mode: 'today' | 'manual'` + `month? / day? / hour?`（今日模式自动取当前时间）
- [ ] `LiurenResult` — `palace / steps(month/day/hour) / path[3]`

## 2. 核心算法（`src/modules/liuren/core/liuren.ts` · **待新建**）

- [ ] `calculateLiuren(input: LiurenInput): LiurenResult` 主入口
- [ ] **今日模式**：`Date.now()` → 当前公历 → `SolarDay → LunarDay` → 取月/日 + 当前时辰序号
- [ ] **手动模式**：直接用传入的 month/day/hour
- [ ] 三步推演：
  - `pos = 0`（起'大安'）
  - `pos = (pos + month - 1) % 6` → P1
  - `pos = (pos + day - 1) % 6` → P2
  - `pos = (pos + hour - 1) % 6` → P3
- [ ] 返回 `{ palace: PALACES[finalName], steps, path: [P1, P2, P3] }`

## 3. 数据（`src/modules/liuren/data/palaces.ts` · **待新建**）

- [ ] `PALACES_ORDER: PalaceName[6]` — 大安/留连/速喜/赤口/小吉/空亡
- [ ] `PALACES: Record<PalaceName, PalaceReading>` — 6 宫详细解读
  - 每宫含：吉凶 + 五行 + emoji + 5 维度解读（总体/事业/爱情/财富/健康）+ 宜列表 + 忌列表
  - **数据规模**：6 × 5 = 30 段解读 + 6 × 2 = 12 组宜忌
- [ ] `data/symbols.ts` — emoji 映射（大安🌿 / 速喜🐦 / 留连🌒 / 赤口⚔ / 小吉🌸 / 空亡🌑）

## 4. UI 组件（`src/modules/liuren/components/` · **待新建**）

- [ ] `InputCard.vue` — 起卦输入（"今日此刻" / 手动月日时切换）
- [ ] `RotateRing.vue` — 6 宫圆环旋转动画（CSS keyframes 或 SVG transform，三步分别快速高亮，最后定格 P3）
- [ ] `ResultCard.vue` — 宫位名 + 吉凶徽章 + 五行 + 符号
- [ ] `AspectTabs.vue` — 5 维度解读 tab 切换（总体/事业/爱情/财富/健康）
- [ ] `SuitableAvoidList.vue` — 宜/忌并排展示（emoji + 文字）
- [ ] **复用** `CollapsibleSection`

## 5. Composables

- [ ] `composables/useLiuren.ts` — `(input: Ref<LiurenInput>) => { result, loading, error }`
- [ ] `composables/useNow.ts` — 提供当前农历月/日/时（每分钟 tick，用于"今日此刻"模式）
- [ ] 接入 `useDivinationStore` 缓存（key = moduleId + (month/day/hour) hash）

## 6. Page（`src/modules/liuren/LiurenPage.vue` · **替换占位**）

- [ ] 国风 / 简约 双主题分支
- [ ] 复用 `useSkeletonReveal`（旋转环动画 ~1.5s 后揭示结果区）
- [ ] 标准布局：InputCard → RotateRing(动画) → ResultCard → AspectTabs + SuitableAvoidList → action-bar

## 7. i18n 文案

- [ ] `locales/modules/liuren.zh-CN.ts`（待新建，注册到 `loadModuleLocale`）
- [ ] `locales/modules/liuren.zh-TW.ts`
- [ ] `locales/modules/liuren.en.ts`
- [ ] 文案：pageTitle / pageSubtitle / breadcrumb / inputCardTitle / mode(today/manual) / btn(qigua/repaipan/share) / palace(6 名) / aspect(5 名) / suitable / avoid / skeleton

## 8. 路由

- [x] `/liuren` 路由已注册
- [x] `LiurenPage.vue` 已 lazy load

## 9. 数据工程

- [ ] **Day 0.5**：录入 6 宫基础（吉凶 + 五行 + emoji），交叉两个公版来源对照
- [ ] **Day 1**：LLM 生成 30 段维度解读 + 12 组宜忌（每段约 50 字）
- [ ] **Day 1.5**：100% 人工审校 + 措辞统一

## 10. 性能

- [ ] `data/palaces.ts` 静态 import（< 8 KB）
- [ ] 仅在"今日此刻"模式 import `tyme4ts.SolarDay`，手动模式 0 依赖
- [ ] **目标**：`/liuren` 路由首屏 gzipped < 90 KB（最轻模块）

## 11. 测试要点

- [ ] 三步推演：起'大安' + month=5 + day=20 + hour=7 → 验证最终命中宫位
- [ ] 今日此刻模式与手动模式输入相同月/日/时 → 结果一致
- [ ] 圆环动画在主流浏览器 60fps
- [ ] 子时（23:00-01:00 跨日）边界（采用早子规则）

## 12. 验收清单

- [ ] 6 宫数据全部录入 + 审校
- [ ] 30 段维度解读 + 12 组宜忌覆盖完整
- [ ] 圆环动画三步高亮路径清晰可识
- [ ] "今日此刻"按钮与"手动起卦"两种模式均可用
- [ ] 主题 / 语言切换无破坏

## 13. 知识库映射

| 层 | 类型 | 来源 | 当前状态 |
|---|---|---|---|
| 算法 | 自研（5 行） | — | 缺 |
| 6 宫名 + 吉凶 + 五行 + emoji | B 类 | 公版命理书 | 缺 |
| 30 段维度解读 | C 类 | LLM × 30 + 100% 审校 | 缺 |
| 12 组宜忌 | C 类 | LLM × 12 + 100% 审校 | 缺 |

## 14. 工时估算

| 任务 | 估算 |
|---|---|
| 6 宫基础数据录入 | 0.3 |
| 30 段解读 + 12 组宜忌 LLM + 审校 | 0.7 |
| 类型 + `core/liuren.ts`（核心 5 行）| 0.2 |
| 5 个 UI 组件（含圆环动画） | 0.6 |
| `LiurenPage.vue` + i18n + 联调 | 0.2 |
| **合计** | **2.0 天** |

## 15. 风险与对策

| 风险 | 应对 |
|---|---|
| 时辰划分（子时跨日） | `tyme4ts` 默认早子时规则 |
| 月份：用农历月 vs 月令 | 采用农历月份（普及版） |
| "起卦方式"争议（民间多版本） | 文档说明本站采用"以报时数定卦"的简化版本 |
| 圆环动画在低端机型卡顿 | 用 `transform` 而非 `top/left`，启用 GPU 加速 |

