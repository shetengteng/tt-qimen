# 16 · 周公解梦模块 · TODO（功能视角）

> 对应设计文档：[`../modules/16-周公解梦模块.md`](../modules/16-周公解梦模块.md)
> 状态约定：详见 [`.cursor/rules/todo-checkbox.mdc`](../../.cursor/rules/todo-checkbox.mdc)（`✅` 已完成 · `⏳` 待办 · `🚧` 进行中 · `⏸` 暂缓 · `🚫` 阻塞）
> **本版重写**：2026-04-23 · 按真实代码完成度同步（MVP 主流程已落地，剩余为数据扩充与体验打磨）
> **2026-04-23（晚）数据工程落地**：
> 1. 新增 `design/jiemeng/raw/`（8 类公版古籍原料 130 条）+ `scripts/jiemeng/build-dreams.mjs`（扫描/校验/emit 三合一）+ `data/dreams.generated.ts`（130 条结构化产物，尚未接入 core）
> 2. 敏感词过滤 T-16.4 完成（23 词 + `hasSensitiveContent` + 三语 `sensitiveHint`）
> 3. **目录对齐 `design/bazi/` 同构**：`raw/` 从 `src/modules/jiemeng/data/raw/` 迁移至 `design/jiemeng/raw/`（build 脚本 `RAW_DIR` 同步更新），区分"设计空间原料 vs 运行时产物"
>
> **2026-04-24 启动数据扩量**：先 C 后 A 路径合并执行（中文 only · LLM 起草 + Codex 审校）
> - 工作流：`design/jiemeng/drafts/{01..08}-*.md` 由 AI 起草 → Codex 审校 → 修订 → build 脚本合并 raw + drafts → `dreams.generated.ts`
> - **raw 扩量（A 阶段）**：基于维基文库《周公解夢》(CC0/PD · 27 类 674 条 8 字断语) + 《梦林玄解》聚合，按 `design/jiemeng/PLAN-500.md` 由 130 条扩到 **495 条**（全 8 类），原 `extracted/` 留存全文供溯源 ✅
> - **drafts 起草（C 阶段）**：与 raw 一一对齐起草 `summary / modern / advice`，**8/8 类 · 495/495 条 ✅ 起草完成**（首批 130 条复用 + 二期 365 条新撰），自检通过（标题与 raw 严格对齐，0 typo）
>
> **2026-04-24（晚 · T-16.10 闭环）审校与运行时切换**：
> 1. Codex 审校完成，产出两份归档：`design/jiemeng/drafts/Claude审校报告.md` + `Claude逐条审校清单.md`（5 必须 / 8 建议 / 20 风格清理）
> 2. 按报告修订 drafts 33 条（5 条 summary/modern 对齐 raw + 8 条 advice 软化 + 20 条风格收紧），8 文件 +48/−48，commit `a9d76a8`
> 3. **build 脚本升级**：新增 drafts 解析器（summary / modern[] / advice 提取 + orphan 告警），`build-dreams.mjs` 现一体读 raw + drafts，**drafts 覆盖率 495/495 = 100%**
> 4. **重 build**：`dreams.generated.ts` 130 → **495**（含 summary / modern[] / advice 完整三件套）
> 5. **core 切换**：`core/jiemeng.ts` import 切到 `DREAM_ENTRIES_GENERATED`；`vue-tsc --noEmit` 通过、`npm run build` 通过
> 6. **死代码清理**：旧 38 条手写 `src/modules/jiemeng/data/dreams.ts` 从 git 移除（git 历史可回溯）
> 7. **T-16.10 收尾完成**，数据通路：raw + drafts → build-dreams.mjs → dreams.generated.ts → core/jiemeng.ts → UI

> **2026-04-24（晚）安全基线说明**：神怪/其他篇含死/丧/鬼/尸/杀/被杀/自杀/受刑/入狱/见血等极端意象（多源自《周公解梦》兵刑章节），drafts 全程以"古籍反吉象 + 情绪/边界/告别视角"解读，advice 不引导任何现实极端行为；其中"梦见自杀"条目额外加入专业援助路径提示。

---

## 0. 模块定位

周公解梦词典 — 搜索梦境关键词 / 分类浏览 → 返回古籍原文 + 现代心理学解读 + 建议。
- 可行性 21/25 分 🟡 可做。
- 体验定位：**搜索型工具** — "梦见 X 是什么意思？"即查即得。
- 当前实现：`src/modules/jiemeng/` 已落地 1 个 Page + 5 个组件 + Store + Core + 8 类元数据 + **495 条词条（含 modern+advice，2026-04-24 晚切换到 generated 生效）**；MVP 主流程 + 495 条数据集全量生效。
- 数据工程：`design/jiemeng/raw/` 公版古籍原料 **495 条**（8 类）+ `design/jiemeng/drafts/` 现代解读 **495 条**（与 raw 严格 1:1，已通过 Codex 审校）+ `dreams.generated.ts` **495 条结构化产物（已接入 core）** + 敏感词过滤 50 词。

### 🔍 当前 backlog 速览（2026-04-24 晚盘点）

| 分类 | 条目 | 结论 | 触发条件 |
|------|------|------|----------|
| **🚫 无效决策** | T-16.6 搜索 debounce | 已评估保持现状（实时 computed + 按钮控显示态） | — |
| **⏸ 门槛未到暂缓** | T-16.5 fuse + 大词典 dynamic import | 当前 gzip 110 KB 可接受 | 词条数 ≥ 1K 条 |
| **⏸ 门槛未到暂缓** | T-16.9 `FortuneError` 异常通道 | 当前算法路径无 IO | T-16.5 改造懒加载 JSON 后同步补 |
| **⏸ 无明确收益暂缓** | T-16.8 抽离独立 `EmptyState.vue` | 内联 20 行可用，无复用场景 | 有第二处页面需要复用 / 或空态逻辑复杂化 |
| **⏸ 低优先** | `useDivinationStore` 按 query hash 缓存 | fuse 本身已毫秒级 | 实测搜索 > 50ms 再补 |
| **✅ 已清理** | 手写 `data/dreams.ts`（38 条） | 2026-04-24 晚 generated 切换后已从 git 移除（历史可 git revert 回溯） | — |

> **结论**：MVP + 数据全量生效 + 敏感词扩充 + 审校闭环**全部已交付**。所有剩余项要么已有决策、要么有明确触发门槛、要么低优先；不存在"卡住的遗留工作"。

---

## 1. 功能边界与完成度（核心）

| # | 功能维度 | MVP 范围 | 状态 | 实现位置 | 缺口 |
|---|---------|---------|------|---------|------|
| 1 | **搜索框** | 关键词输入 + 分类下拉 + 查询/清空按钮 | `[x]` | `components/DreamInput.vue` | `store.query` 已实时双向绑定 → `displayItems` 走 `computed(searchDream)` 实质上**已是实时搜**；按钮仅控"显示结果区 + 入历史 + 平滑滚动"。T-16.6 已评估为"保持现状"（见 §5） |
| 2 | **模糊搜索算法** | fuse.js 多字段（keywords/title/summary）加权 | `[x]` | `core/jiemeng.ts` | 已实现（keywords 0.55 / title 0.3 / summary 0.15，threshold 0.35）；包**已安装**（2026-04-23 npm i） |
| 3 | **分类浏览** | 8 大类（动物/人物/自然/身体/生活/神怪/建筑/其他） | `[x]` | `data/categories.ts` + `types/index.ts` | 已与设计文档"9 大类"对齐到原型实际的 **8 大类**（去掉了"植物/物品/活动"，新增"人物/身体"） |
| 4 | **分类网格** | 8 张大卡片 + 实时计数，点击过滤 | `[x]` | `components/CategoryGrid.vue` | 计数走 `getCategoryCounts()` 自动推导 |
| 5 | **搜索结果列表** | title + summary 预览 + 标签 + 关键词高亮 | `[x]` | `components/DreamResultList.vue` | 含 `<mark>` 高亮、HTML 转义、tag badge |
| 6 | **详情卡** | 古籍原文 + 现代解读多段 + 建议 + 警示 | `[x]` | `components/DreamDetail.vue` | 内嵌卡片（非 dialog）便于分享卡截图 |
| 7 | **历史搜索** | localStorage 最近 5 次 | `[x]` | `stores/jiemengStore.ts` + `components/RecentSearches.vue` | `useStorage` 持久化，去重 + 上浮 |
| 8 | **现代化建议**（top 500 词条） | LLM 生成 + 审校 | `[x]` | `dreams.generated.ts`（495 条 summary/modern/advice，2026-04-24 晚接入 core） | 全量闭环：AI 起草 → Codex 审校 → 33 条修订 → build 合并 → 运行时生效 |
| 9 | **敏感词过滤** | 死亡/鬼神/疾病等词柔性文化提示（不屏蔽） | `[x]` | `data/sensitiveWords.ts`（50 词） + `core/jiemeng.ts#hasSensitiveContent` + `DreamDetail.vue` | 命中时在详情底部追加 `sensitiveHint` 提示；三语文案完整（2026-04-23 首版 23 词 → 2026-04-24 扩到 50 词覆盖兵刑章节） |
| 10 | **结果分享** | 词条 PNG 导出 | `[x]` | `JiemengPage.vue` 接入 `useShareCard` + `ShareToast` | 双主题分享卡均已接 |
| 11 | **空态** | 搜索无结果时引导浏览分类 | `[x]` | `DreamResultList.vue` 内联 | 已有 icon + title + desc 三层引导（未拆独立组件） |

---

## 2. 交互流程完成度（设计文档 §4 + 原型图 §4.8）

| 步骤 | 期望体验 | 状态 | 备注 |
|------|---------|------|------|
| 1. 进入 `/jiemeng` | 路由懒加载 | `[x]` | router 已注册（`src/router/index.ts`） |
| 2. 看到搜索框 + 分类入口 | 搜索框 + 8 分类网格 | `[x]` | 双入口已就位（双主题） |
| 3. 输入"蛇" | 点击查询后返回结果 | `[x]` | 内部已实时计算；按钮触发"暴露结果区 + 写入历史 + 滚动"，T-16.6 评估保持此设计 |
| 4. 看到结果列表 | title + 预览 + 关键词高亮 | `[x]` | `<mark>` 高亮已实现 |
| 5. 点击某条 | 详情卡展开完整 content + advice | `[x]` | 平滑滚动到详情区 |
| 6. 点击分类 | 进入分类筛选（按 category 过滤） | `[x]` | 再次点击同类则取消筛选 |
| 7. 历史搜索 | 显示最近 5 次（点击重新搜索） | `[x]` | 可清空、可单击回填 |
| 8. 空态 | "没找到？试试浏览分类" | `[x]` | 文案已译三语 |
| 9. 一键分享 | 词条 PNG 导出 | `[x]` | 接通用层 `useShareCard` |

---

## 3. 子功能详情

### 3.1 算法（设计文档 §3）

| 步骤 | 状态 | 备注 |
|------|------|------|
| 启动时 import `fuse.js` + 词条数据 | `[~]` | 当前为**静态 import**（页面懒加载已带来分包），尚未做 dynamic import；待数据扩充到 K 级再切换 |
| Fuse 初始化（keys 加权 + threshold 0.35） | `[x]` | `getFuse()` 单例懒构建，首次 search 时才建索引 |
| `searchDream(query, opts)` | `[x]` | 支持 `category` 子集过滤 + `limit`（默认 10） |
| `getDreamsByCategory(category)` | `[x]` | 返回 `DreamCategoryResult` |
| `getCategoryCounts()` | `[x]` | 8 类计数，供网格展示 |
| `getDreamById(id)` | `[x]` | 供选中态、最近搜索回显 |
| 历史搜索写入 localStorage（最近 5 次） | `[x]` | `useStorage` + 去重上浮 |
| 搜索 debounce 300ms | `[/]` | 评估暂缓（T-16.6 已结论保持现状）；内部本质实时，仅显式动作走按钮 |
| 异常态外抛 `FortuneError` | `[ ]` | 当前算法路径无 IO，无明显报错点；扩到懒加载 JSON 时再补 |

### 3.2 UI 组件清单

| 组件 | 用途 | 状态 |
|------|------|------|
| `JiemengPage.vue` | 整页装配（双主题：guofeng / minimal） | `[x]` |
| `DreamInput.vue` | 关键词 + 分类下拉 + 查询/清空 | `[x]`（query 实时双向；按钮控显示态，T-16.6 已结论保持） |
| `CategoryGrid.vue` | 8 分类大卡片 + 计数 | `[x]` |
| `DreamResultList.vue` | 搜索/分类结果列表 + 高亮 + 空态 | `[x]` |
| `DreamDetail.vue` | 详情（classical + modern + advice + warning） | `[x]` |
| `RecentSearches.vue` | 最近 5 次搜索 chip + 清空 | `[x]` |
| `EmptyState.vue` | 无结果空态 | `[/]` 已内联在 `DreamResultList.vue`，未拆独立组件 |

### 3.3 i18n

| 键 | 状态 |
|----|------|
| 模块名 / 标题 / 搜索提示 | `[x]` 三语完整（zh-CN ~1058 / en ~878 / zh-TW ~875） |
| 8 分类名（`jiemeng.category.{key}`） | `[x]` 三语完整 |
| 历史搜索 / 空态文案 | `[x]` 三语完整 |
| 标签文案（auspicious/cautious/ambiguous/neutral） | `[x]` 三语完整 |
| **梦境词条文本**（title/classical/modern/advice） | `[/]` **不翻译**（古文/中文保留），仅 UI 框架文案译 |
| 现代化建议批量扩充 | `[x]` 495 条全部带 summary/modern/advice，已通过 Codex 审校并生效（2026-04-24 晚） |

---

## 4. 跨模块依赖

| 依赖项 | 状态 | 说明 |
|--------|------|------|
| `fuse.js` 包 | `[x]` | **2026-04-23 已安装**（npm i），`package.json` 声明 `^7.3.0` |
| `useDivinationStore` 缓存（按 query hash） | `[ ]` | 优先级低（fuse 本身已快） |
| `useShareCard` + `ShareToast` | `[x]` | 已接，双主题分享卡可用 |
| `useDebounceFn`（VueUse） | `[/]` | 已装但本模块**未使用**（按钮触发） |
| `useStorage`（VueUse） | `[x]` | 已用于历史搜索持久化 |

> **不依赖** BirthInput — 本模块只需搜索词，与生辰无关（已在 store 注释中固化）。

---

## 5. 后续推进任务（开放 Backlog）

> 顺序按推荐优先级；标注预估工时。

- [x] ✅ T-16.1 数据扩充：**放弃 HF `hwl985/fortune-telling`**（经 2026-04-23 核查，该集是通用命理 QA 207 行，**非解梦词典**）；改为自建 `design/jiemeng/raw/*.md` 公版古籍原料，先落地 130 条（2026-04-23），二期按 `design/jiemeng/PLAN-500.md` 扩到 **495 条**（2026-04-24）
- [x] ✅ T-16.2 数据构建脚本：`scripts/jiemeng/build-dreams.mjs`（dry-run 校验 / --emit 产出 `dreams.generated.ts` / 统计分类分布 / lint id 去重），zero deps（完成 2026-04-23）
- [x] ✅ T-16.3 top 500 词条 `summary / modern / advice` 起草 + Codex 审校 + 修订闭环：`design/jiemeng/drafts/` 全 8 类 · **495/495 起草 + 审校 + 修订完成**（2026-04-24，commit `a9d76a8` 落地 33 条修订）
- [x] ✅ T-16.4 敏感词过滤首版：`data/sensitiveWords.ts` 23 词（死/亡/丧/尸/棺/坟/鬼/疾/病/血/忧/哭…）+ `core/jiemeng.ts#hasSensitiveContent` + `DreamDetail.vue` 追加 `sensitiveHint` 柔性提示（不屏蔽）+ 三语 i18n（完成 2026-04-23）
- [x] ✅ T-16.4-ext 扩充敏感词到 **50 词**（生/殁/殇/祟/殉/咒/狱/枷/缚/刑/刀/剑 等延伸；覆盖兵刑章节意象）（完成 2026-04-24，commit `3b9def1`）
- [ ] ⏳ T-16.5 fuse + 大词典 dynamic import 改造（数据 ≥ 1K 条后启动；当前 495 条暂缓）· 0.2 天
- [/] ⏸ T-16.6 搜索 300ms debounce 实时搜方案评估 — **2026-04-24 评估完成：保持现状**
  - 现状真相：`store.query` 已经实时双向绑定，`displayItems = computed(searchDream)` 已经是事实上的实时搜，**计算路径毫秒级、不需要 debounce**；
  - "按钮触发"的真正语义只是「显示结果区 + 写入历史搜索 + 平滑滚动」三件事，不是"延迟搜索"；
  - 切实时暴露结果区会带来反作用：
    * 中文 IME composition 期间逐字暴露结果会闪烁与误触历史；
    * 历史搜索会被 1-2 字噪声词污染；
    * `scrollIntoView` 不能每次 input 都触发；
  - 结论：**保留按钮触发**，待 T-16.10 接入 495 条且数据稳定后再观测真实使用反馈；如要优化，建议方向为
    * 用 `@compositionstart/@compositionend` 屏蔽 IME 中间态；
    * 仅当 `query.length >= 2` 且非 IME composing 时实时滚动；
    * `pushRecent` 仍由"按 Enter / 点查询 / 点结果"三种确定意图触发，不绑 input。
- [x] ✅ T-16.7 数据来源 license 注记：`design/jiemeng/raw/README.md` 明确古籍公版声明 + 排除项（HF/ducenand/leochan2017 的不适用理由），MIT 同项目（完成 2026-04-23）
- [x] ✅ T-16.10 升级 `build-dreams.mjs` 合并 drafts（`summary/modern/advice`）→ 重 build `dreams.generated.ts` 至 **495 条（drafts 覆盖 100%）** → `core/jiemeng.ts` 切 `DREAM_ENTRIES_GENERATED` → vue-tsc / npm run build 通过 → 手写 `dreams.ts` 38 条已从 git 移除（完成 2026-04-24 晚）
- [/] ⏸ T-16.8 抽离独立 `EmptyState.vue`（当前内联在 `DreamResultList.vue`，无紧迫性）
- [/] ⏸ T-16.9 `FortuneError` 异常通道（当前算法路径无 IO，待懒加载 JSON 落地后再加）

---

## 附录 A · 知识库与数据来源（设计文档 §6）

| 资产 | 类型 | 来源 | 规模 | 状态 |
|------|------|------|------|------|
| 词条数据（运行时 · 已清理） | B 类 | 人工精选（`data/dreams.ts`） | 38 条（含 advice） | `[x]` 2026-04-24 晚 T-16.10 切换后从 git 移除；历史可 git revert 回溯 |
| 词条数据（工程原料） | B 类 | 公版《周公解梦》+《梦林玄解》(`design/jiemeng/raw/*.md`) | **495 条 · 8 类** | `[x]` 2026-04-24 扩到 495 |
| 词条数据（drafts 现代解读） | C 类 | AI 起草 + Codex 审校 + 33 条修订（`design/jiemeng/drafts/*.md`） | **495 段 · 8 类**（summary/modern/advice 三件套） | `[x]` 2026-04-24 审校完成并修订（commit `a9d76a8`） |
| 词条数据（运行时 · 生效） | B 类 | `build-dreams.mjs` 合并 raw + drafts 产出 `dreams.generated.ts` | **495 条（drafts 覆盖 100%）** | `[x]` 2026-04-24 晚接入 core，已成为唯一运行时源 |
| 分类体系 | B 类 | 传统《周公解梦》8 大类（与原型对齐） | 8 类 | `[x]` |
| 敏感词过滤词典 | B 类 | 自维护（`data/sensitiveWords.ts`） | 50 词（覆盖兵刑章节） | `[x]` 2026-04-23 首版 23 → 2026-04-24 扩到 50 |

**实际获取路径**（2026-04-23 决策更新）：

1. ~~Hugging Face `hwl985/fortune-telling`~~ — 经核查为通用命理 QA（混杂八字/紫微/塔罗/面相），**不是解梦词典**，放弃
2. ~~GitHub `ducenand/JieMeng`~~ — 仅爬虫脚本，抓取源 `tools.2345.com` 存在版权风险，放弃
3. ~~GitHub `leochan2017/zgjm`~~ — 数据被作者移除（只剩彩蛋 2 条），不可用
4. ✅ **采用**：自维护 `design/jiemeng/raw/*.md` 的公版古籍结构化语料（《周公解梦》+《梦林玄解》）
5. 由 `scripts/jiemeng/build-dreams.mjs` 扫描 → 校验 → 合并 drafts → 产出 `dreams.generated.ts`
6. ✅ T-16.10 已收尾（2026-04-24 晚）：generated 495 条含完整 summary/modern/advice，core 已切 import，手写 `dreams.ts` 退役保留
7. 规模到 ≥ 1K 条后触发 T-16.5 懒加载改造

## 附录 B · 数据工程

- [x] ✅ 数据工程目录结构：`design/jiemeng/raw/` + `README.md`（含 license 注记与数据源决策说明）（完成 2026-04-23）
- [x] ✅ 公版古籍语料 8 份首版：`raw/01-animal.md` … `raw/08-other.md`（130 条，分类 15-18/类）（完成 2026-04-23）
- [x] ✅ 数据构建脚本：`scripts/jiemeng/build-dreams.mjs`（dry-run / --emit，native Node 20+，无依赖）（完成 2026-04-23）
- [x] ✅ 敏感词词典首版（23 词，柔性提示不屏蔽）（完成 2026-04-23）
- [x] ✅ Drafts 目录约定：`design/jiemeng/drafts/README.md`（与 raw 严格分层、字段约束、Codex 审校 prompt 模板）（完成 2026-04-24）
- [x] ✅ Drafts · 全 8 类首批 130/130 起草完成（`drafts/01-animal.md` … `drafts/08-other.md`，复用 38 条手写样本 + 92 条新撰）（完成 2026-04-24）
- [x] ✅ Extracted 公版语料汇总：`design/jiemeng/extracted/zhougong-wikisource.md`（27 类 674 条 8 字断语，CC0/PD，含原文链接）+ `extracted/README.md`（完成 2026-04-24）
- [x] ✅ 扩量计划：`design/jiemeng/PLAN-500.md`（聚合原则、按类目标、敏感意象处理基线）（完成 2026-04-24）
- [x] ✅ Raw 扩量 130 → **495**（动物 67 / 人物 63 / 自然 64 / 身体 54 / 生活 54 / 神怪 45 / 建筑 63 / 其他 85），全部 build 通过（完成 2026-04-24）
- [x] ✅ Drafts 扩量 130 → **495**（与 raw 严格一一对齐 8 类 · 0 typo），含神怪/兵刑章节安全基线（完成 2026-04-24）
- [x] ✅ Codex 审校报告落地：`drafts/Claude审校报告.md` + `drafts/Claude逐条审校清单.md`（5 必须 / 8 建议 / 20 风格清理，合计 33 条）+ drafts 33 条修订 commit `a9d76a8`（完成 2026-04-24 晚）
- [x] ✅ build-dreams.mjs 升级：扫描 `drafts/*.md` 按 `### 标题` 匹配合并 `summary/modern[]/advice`，新增 drafts 覆盖率统计 + orphan 告警（完成 2026-04-24 晚）
- [x] ✅ 重 build：`dreams.generated.ts` 130 → **495**（drafts 覆盖 495/495 = 100%，summary/modern/advice 全字段完整）（完成 2026-04-24 晚）
- [x] ✅ core/jiemeng.ts 切换 import 到 `DREAM_ENTRIES_GENERATED`（T-16.10 收尾，vue-tsc + npm run build 均通过）（完成 2026-04-24 晚）
- [x] ✅ 清理死代码：移除 `src/modules/jiemeng/data/dreams.ts`（38 条手写，已退役）（完成 2026-04-24 晚）
- [x] ✅ 敏感词扩展到 **50 词**（生/殁/殇/祟/殉/咒/狱/枷/缚/刑/刀/剑 等延伸）（完成 2026-04-24，commit `3b9def1`）
- [x] ✅ 数据来源 license 注记（古籍公版声明 + 排除项记录）（完成 2026-04-23）

## 附录 C · 工时估算（设计文档 §7）

| 任务 | 工时 | 状态 |
|------|------|------|
| 数据工程目录 + 公版古籍原料 130 条 + 构建脚本 | 1.0 天 | `[x]`（完成 2026-04-23） |
| Raw 扩量 130 → 495（8 类，含 8 commits 分类落地） | 1.0 天 | `[x]`（完成 2026-04-24） |
| Drafts 起草 495 条 modern/advice（中文 · 8 commits 分类落地） | 1.5 天 | `[x]`（完成 2026-04-24，38 条复用 + 457 条新撰） |
| 安装并集成 fuse.js | 0.2 天 | `[x]`（完成 2026-04-23） |
| `core/jiemeng.ts`（fuse 初始化 + 搜索 + 历史） | 0.5 天 | `[x]` |
| UI 组件 6 个 + 主题双套 | 1.5 天 | `[x]` |
| 详情弹窗 + 敏感词过滤 + i18n | 0.5 天 | `[x]`（敏感词过滤 2026-04-23 完成） |
| Codex 审校 + 修订（C 阶段收尾） | 0.5 天 | `[x]`（完成 2026-04-24 晚，33 条修订 commit `a9d76a8`） |
| build-dreams.mjs 升级 + 重 build + core 切 import（T-16.10 收尾） | 0.5 天 | `[x]`（完成 2026-04-24 晚） |
| 敏感词扩展到 50（T-16.4-ext） | 0.1 天 | `[x]`（完成 2026-04-24 晚，commit `3b9def1`） |
| **合计** | **~7.3 天** | **全部闭环**：MVP + 495 词条（古籍 + 现代解读 + advice）+ 敏感词 50 + 运行时生效，所有数据工程 / 审校 / 切换任务已交付 |

## 附录 D · 风险与对策（设计文档 §8）

| 风险 | 应对 | 当前处理 |
|------|------|---------|
| 搜索性能（9000 条 × 多字段模糊） | fuse.js 索引化；> 3000 条时考虑 Web Worker | 当前 38 条无压力；generated 130 条备用；扩量后再压测 |
| 数据集错漏（编码、重复） | 抽样校验 + 去重脚本 | `build-dreams.mjs` 内置 lint：id 去重、keywords 至少 2 项、tag 白名单、分类白名单 |
| 迷信引导风险 | 每条详情底部加"梦境解读仅为文化参考，不作行为指导" | `[x]` 已在 `DreamDetail.vue` 底部添加警示文案 |
| 敏感词（死亡、鬼神、疾病） | 柔性文化提示（不屏蔽），引导象征视角阅读 | `[x]` 2026-04-23 完成：`data/sensitiveWords.ts` 23 词 + `hasSensitiveContent()` + `DreamDetail` 追加 `sensitiveHint` + 三语 i18n |
| `fuse.js` + 数据集首屏体积膨胀 | dynamic import；进入页面才加载 | 当前为静态 import，靠路由懒加载分包；JiemengPage chunk 201 KB（gzip 110 KB · 495 条 + fuse），≥ 1K 条后再切 dynamic import（T-16.5） |
| 多语种梦境词条 | 古文不译；只译 UI 框架与现代化建议 | `[x]` 古文/词条文本固化为中文，UI 文案三语 |
| 数据源版权 | 使用公有领域古籍 + 注记 `design/jiemeng/raw/README.md` | `[x]` 古籍公版（《周公解梦》《梦林玄解》）+ 自维护关键词标注（MIT） |
